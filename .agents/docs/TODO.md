# Project To-Dos

Items extracted from `JOURNAL.md` during `good-sleep` consolidation, plus follow-ups recorded directly while working. Keep this file as an active backlog only: once an item is addressed, move its work summary into `JOURNAL.md` and remove it from here.

## Open Items

### Documentation

- [ ] **distill-2026-09-13-synthesis-findings**: promote three durable conclusions from the synthesis refresh into the canonical agent documents. `ARCHITECTURE.md` should state that commit completion can precede prefix visibility and that read-your-writes is the explicit acknowledged-version, `wait_visible`, exact-snapshot sequence. `OVERVIEW.md` should state the context-eligibility plane and BYOC-first boundary, including the deliberate exclusions of general DBaaS, OLAP, vector search, and authorization-graph scope. `QUALITY_GATE.md` should require CI toolchain parity, exact AddressSanitizer target scope, literal concurrency namespaces for reusable workflows, and completion of the actual remote job graph before CI is called proven. -- *source: JOURNAL `2026-09-13 — Deep-sleep refresh for visibility, CI evidence, and context eligibility`*

- [x] **stored-page-crcs-are-not-verified** ( *scan half closed 2026-09-06; **read-path cache landed 2026-09-14** -- both halves now closed* ) **KEEP decision taken by the maintainer 2026-09-14, after the cost was on the table rather than before.** The question asked was whether to drop it, and the grounds were not performance: the cache forces `Snapshot::load` to **error** on a corrupt node or payload where it previously returned data or a silently short set, which is a breaking change to the primary read API that the feature compelled rather than chose. Kept because that moves `load` toward the behaviour the crate already commits to elsewhere -- `KeyStream`'s header ( "a short answer presented as a complete one is the worse failure" ), `ChunkRef::validate` refusing reserved bits, `SuperBlock::decode` refusing an unknown geometry. Before this, `load` was the outlier. A `DbOptions` switch was considered and rejected: two paths where one is rarely exercised is the shape this repository keeps finding rotted. **Do not reopen on throughput grounds** -- reopen only if `load` should become total again, which is a product decision about failure behaviour.
: **`SegmentedMmap::verify_once` recomputes a stored checksum the first time a region is faulted in and not again until a write lands on it**, keyed on file offset and invalidated by `write_at` evicting every intersecting entry. All three families are checked on the read path: index nodes in `NodeReader::node`, standalone extents in `read_container_for` ( where the trailer was already being read for `ckey_tag`, so the CRC costs no extra read ), and packed pages once per page rather than once per chunk.

  **The check and the error channel had to land together, and that is the finding.** `merged_chunks` returned a bare `Vec` and swallowed both failure paths, so the first working version turned a corrupt index node from *silently correct* into *silently empty* -- strictly worse than not checking. It is now fallible, which also aligns `load` with `KeyStream`, whose header already called a short answer presented as a complete one the worse failure. **A check whose error has nowhere to go is not a check.**

  **`Db::verify` needed a non-verifying reader**, added as `RawNodes` -- stateless rather than a mode flag, because a scan built on the refusing reader aborts at the first bad page and attributes nothing. Contract change, deliberate and user-visible: `Snapshot::load` now errors on a corrupt node or payload where it previously returned data or a short set. Original entry follows.

  Original: ( *the scan half closed 2026-09-06; the read-path cache remains* ): **`Db::verify()` now recomputes all three families** — B+tree nodes ( inside `rebuild`, which already holds every node through its `NodeReader` ), packed pages and standalone extent trailers ( via `Rebuilt::checksum_violations`, wired into `Db::verify` ). Each family is tested twice, a byte flipped in the **covered region** and a byte flipped in the **checksum field**, because those fail in opposite directions.

**The wiring needed its own test, and finding that out is the reusable part.** `checksum_violations` was implemented and fully unit-tested while nothing called it — and removing the one line in `Db::verify` left **every test green**, because the unit tests call it directly and the end-to-end tests covered only the index-node family, which `rebuild` verifies without going through it. `durability::a_corrupt_standalone_payload_fails_a_real_verify` is the test that closes it, and sabotage shows it is the *only* thing that reddens. Same shape as `stored-superblock-descriptors` the same day: the check and its call site are two changes, and only the second refuses anything.

**Two doc comments were found to be false, which is worse than absent** — both asserted a mechanism that did not exist. `PackedHeader::verify` said it was "called once per fault-in and memoized" and was called by nothing; `LeafRef::verify` said "the production checksum check is in `store::checksum` on the page-read path" and there was no such check. Both corrected. `index/tree.rs`'s duplicate `OFF_CRC` / `compute_crc` were deleted in favour of `index/node.rs`'s, so a leaf and an internal node cannot drift.

**An ordering bug surfaced while implementing**: the node walk had to move *ahead* of the entry walk, because a corrupt node otherwise cascades into bogus "unallocated slab" errors, or aborts the walk through `tree.iter`'s `?` before any checksum runs.

**Fatal versus fallback follows the existing `slabmeta` policy rather than a new one.** Slab metadata is the only checksum that falls back, because it is a cache of state the committed index reproduces. None of these three is derivable — the node is the authority on liveness, the other two hold the data — so all three are reported, and `rebuild_allocator_at_open` already refuses to adopt a rebuild that reported any error ( adopting an incomplete liveness map does not fail to repair, it frees live data ). Deliberately **not** wired into `rebuild_allocator_at_open`: a corrupt *payload* does not invalidate the liveness map.

**Remaining: the once-per-faulted-page checksum cache** for the online read path. **Strengthened 2026-09-14**: `miri-cannot-reach-the-mmap-unsafe-sites` measured that neither ASan nor Valgrind can see a read that stays inside the mapping, so for a `ChunkRef` pointing at the wrong in-segment extent this cache is not merely the best check -- it is the **only** one, the sanitizers being structurally blind and `ChunkRef::validate` covering format bits rather than offsets. The design is settled and recorded — it belongs on the mmap segment ( the only thing that materialises file bytes, and whose `write_at` is the only thing that changes them ), keyed on **file offset** rather than a page id ( a freed and reallocated node recycles its id, so an id-keyed entry goes stale silently ), invalidated by `write_at` evicting every intersecting entry, which is exact under I2 because a published extent is immutable. Not self-evident work: the two call sites that know which CRC covers which region both live in `db/store.rs`, and the segment cannot decide alone — it is handed a payload length for an extent and a node size for an index node and cannot tell them apart, and an extent's trailer lies outside the range it is asked for.

Also noted and left alone: `ExtTrailer::ckey_tag` is still not checked by the scan ( only online, and only when slab geometry is known ). Original entry: documenting the on-disk format
  exposed that three families of checksum are written but not recomputed by
  either normal reads or the current integrity scan: B+tree nodes, packed
  pages, and standalone container payloads. Online reads check node shape and
  version, packed-page magic/flags/key range, and standalone chunk-key tags;
  the integrity scan checks reachability, allocator agreement, and container
  invariants. None checks those stored CRC32C values. The standing format
  reference says this explicitly so their presence is not mistaken for
  end-to-end verification. Close this with the intended once-per-faulted-page
  checksum cache plus an integrity scan that recomputes every stored CRC, with
  corruption tests proving each path fails. -- *source: on-disk format audit,
  2026-08-30*

- [ ] **decoded-geometry-does-not-drive-address-calculation** ( *renamed and narrowed 2026-09-12 from `stored-superblock-descriptors-are-not-authoritative`, whose correctness half is closed* ): the allocator's capacity, slot lookup and new-allocation arithmetic use the **compiled** size-class ladder, page size and slab size rather than the ones decoded from the superblock. So the practical on-disk format is fixed to today's default geometry, even though the fields exist to make retuning readable.

**This is no longer a correctness gap, and that is the change.** `SuperBlock::decode` refuses a file whose `class_sizes`, `page_size` or `slab_size` differ from this build's, and `superblock::pick` — the **only** production read path, called from both store-open sites — decodes both slots and propagates that error. So no address calculation can ever run against a geometry the compiled constants disagree with: the allocator reading compiled values is safe because a file carrying anything else never opens. A zero in `page_size` or `slab_size` is tolerated as a pre-comparison file written at the compiled value, following the `node_size` precedent; `shard_id` deliberately gets no such tolerance, because shard 0's number *is* zero and tolerating it would re-open the image-swap hole for every pair involving shard 0.

**Why a decode-level test is sufficient here, unlike for `check_identity`.** That method was a *separate* check a call site had to remember, and it sat with no caller for part of a session — this repository's most-repeated failure class. The geometry comparison lives **inside the decoder**, so there is no second wiring step to forget: nothing can open a shard without decoding its superblock. That is the distinction to apply before demanding an end-to-end test for a validation — ask whether the check can be reached without being called, not merely whether an integration test exists.

**What remains is a capability, not a fix**, and it is gated on a workload: thread one decoded geometry object through capacity, slot lookup and allocation so a non-default ladder can be *honoured* rather than refused, plus a non-default-ladder round-trip test. Do not start it to make the format "properly versioned" — no file the crate has ever written carries a non-default geometry, so today the fields buy a clear refusal at zero cost, which is the honest state until someone needs to retune.
- [ ] **per-key-blob-for-cold-keys**: **premise confirmed by measurement.** `cargo run --release -p yesno-e2e -- --show-output --arg keys=400 --arg per_key=400 --arg rounds=40 --arg spread=100000 e2e/scenarios/aged_state.py` shows the sparse regime allocating *only* the index class — 2 slabs, ~1 961 nodes, **zero payload extents**, because every 1-ordinal chunk is inline in its leaf. That corpus holds 160 000 index entries for 400 keys. One extent and one index entry per cold key instead of one entry per chunk is the designed answer, and the format already reserves the bit for it: `ChunkRef` `[59:60) enc`, "0 = raw Roaring; MUST be 0 in v1".

**A tradeoff the design did not record.** `card_m1` is 16 bits, so a blob covering a whole key cannot carry that key's cardinality — a blobbed key's `cardinality()` must read the blob, where today it is summed from index entries **without touching a payload**. `ARCHITECTURE.md` calls that a headline property ( it is what makes the cardinality identities and DataFusion statistics free ). Blob-ifying trades it for space on cold keys, which is probably right, but it is a decision rather than an oversight and needs to be made deliberately.

  Sketch: blob-ify at checkpoint any key whose per-checkpoint dirty fraction stayed under ~0.2%; store the chunk directory plus payloads in one extent; index it at `ChunkKey(key, 0)` with `enc = 1`; explode back to chunked on the first write that exceeds the threshold. The read paths that need a fallback probe are `Snapshot::disk_container` ( exact `(key, prefix)` lookup ) and `merged_chunks` ( range scan ) — both currently assume one entry per chunk.

Do not land the encoder without the wiring. Eight pieces of machinery in this codebase were built to spec, unit-tested, and connected to nothing; an unwired blob codec would be the ninth. -- *source: design phase, measured 2026-08-25*

### PostgreSQL extension

- [ ] **fdw-two-phase-commit**: `yesno-pg` buffers writes per transaction and flushes them in a `XACT_EVENT_PRE_COMMIT` callback, so `ROLLBACK` is correct and a multi-row statement costs one server commit. A crash **between** that flush and PostgreSQL's own commit record leaves the two disagreeing: yesno has the rows, PostgreSQL does not. There is no `PREPARE` implementation and no claim of one.

  Closing it means implementing the two-phase callbacks against a durable yesno prepare/resolve protocol, which yesno does not currently expose. A prepared `WriteBatch` cannot merely reserve an ordinary commit version: the unresolved slot is a hole in the consecutive-prefix visibility oracle and would stop every later commit becoming visible for as long as PostgreSQL leaves the transaction prepared. A sound design needs durable prepared operations outside that ordered stream, idempotent commit/discard RPCs, and restart reconciliation with PostgreSQL's transaction status before assigning the real yesno version. Worth weighing against just documenting the window: the FDW's writes are an ingest path, and an ingest that occasionally re-sends a batch is idempotent here, because a set holds each ordinal at most once. -- *source: phase 5, 2026-08-29; design audit 2026-08-30*

- [ ] **multiprocess-read-only-reader** ( *restored 2026-09-14; cited by name from two module headers since the FDW landed and recorded nowhere* ): `Db::open` takes a **non-blocking exclusive `flock`** on the database directory and yesno has no shared or read-only open. PostgreSQL forks one backend per connection, so N backends attempting an in-process open means N-1 failures, and a running `yesnod` makes it N. This is why `Transport::Local` does not exist: `yesno-pg` reaches yesno over Flight only, and the `Local { data_dir }` variant is declared but unimplemented.

  Closing it needs more than relaxing the lock. Extent reclamation is enforced from **process-local** state, so a foreign reader is invisible to the reclaimer and its pages can be freed underneath it -- a genuine cross-process reader registry is the load-bearing piece, not the `flock`. That registry has to survive a reader dying without releasing, which is where `reader-registry-pid-reuse` becomes the hard part rather than a detail.

  Do not implement `Transport` for a `data_dir` server by calling `Db::open`. It works for exactly one backend and then fails in a way that looks like a locking bug rather than a design gap; `yesno-pg/src/transport/mod.rs` says so at the site and that warning is the only thing standing between a reader and a plausible-looking mistake. -- *source: recovered from `yesno-pg/src/transport/mod.rs` and `options.rs`, 2026-09-14*

- [ ] **tam-mvcc**: recorded when the table access method lands. The short version: stored xids would buy consistency with heap tables in one query, and would cost freezing, `relfrozenxid`, and ~64x write amplification from bit-planed xids on single-row DML. Do not record it as "MVCC is missing" — what remains missing is agreement with PostgreSQL's clock. The independently useful *within-yesno* half is closed by `tam-pin-snapshot` and `tam-pin-statement`: transaction-level and statement-level Flight tickets now provide the isolation each PostgreSQL level promises. -- *source: planning, 2026-08-29; narrowed 2026-08-30*

### Kernels

- [ ] **kernel-specialization-simd**: all nine kind-pairs are specialized in both `ops::apply` and `ops::card`, and as of 2026-08-25 the last per-value probes are merges too. Measured against the `roaring` crate, worst first: `array x run` OR **5.0x**, `array x run` cardinality **2.15x**, `array x run` AND **2.07x**, `array x bitmap` cardinality **1.44x**; `run x run` cardinality is **35x faster** than the reference and `bitmap x run` **2.3x faster**. Nothing pathological remains. The `array x run` OR/XOR arms expand the run into words on purpose — emitting intervals would need result split/merge, which the design excludes from v1 by name. Changing that is a **design decision**, not a tuning step. True SIMD ( shuffle-based array intersect ) is still a separate later question, benchmark-gated. When specializing a kernel, check whether `ops::card` has the same arm — it is a second implementation of the same dispatch and does not inherit the fix. **RE-MEASURED 2026-09-07 and the recorded figures are current, not stale** — `array x run` OR **5.07x** ( recorded 5.0 ), `array x run` cardinality **2.14x** ( 2.15 ), `array x run` AND **1.99x** ( 2.07 ), `array x bitmap` cardinality **1.40x** ( 1.44 ). Worth knowing given the neighbouring `array-intersect-simd-x86` entry was found over-claiming on the same day: this one is trustworthy. The wins are unchanged too — `run x run` cardinality **0.03x** of the reference, `run x run` AND/OR **0.16x**, `bitmap x run` cardinality **0.40x**.

**And a hypothesis about the AND gap was tested and refuted, recorded so it is not re-derived.** `binary_ops_mixed` builds every run from one contiguous stretch, so its run containers hold **exactly one interval** — the bench file says so and warns against quoting from them. The obvious inference is that `merge_array_run`'s linear scan is being measured on a shape where the answer is a sub-slice reachable by two binary searches, i.e. that the gap is an `nruns = 1` artifact and a `GALLOP_RATIO` path would close it. **It is not.** Measured at m = 4000 against `roaring`, sweeping interval count:

  ```text
  nruns       yesno     roaring   ratio
      1     3459 ns     1665 ns   2.08x
      2     3126 ns     1670 ns   1.87x
      8     3581 ns     1690 ns   2.12x
     64     3588 ns     1744 ns   2.06x
    512     4312 ns     1699 ns   2.54x
   2048     4267 ns     1668 ns   2.56x
  ```

**The ratio is ~2x at every interval count**, and `roaring` is *flat* across the sweep while this crate degrades from 3.5 to 4.3 us. So the deficit is in the constant factor of the merge itself, not in the shape the benchmark happens to build, and a gallop path would at best help the low-`nruns` end. The likely target is that `merge_array_run` pushes element-by-element behind a data-dependent branch, where `simd::merge_and_into` already solves the same problem for array x array with a mask-and-compact shuffle table. That is the direction to try, and it owes its own measurement.

**Largely closed 2026-09-07 — and the fix was not the vectorization the line above predicted.** Attributing the cost first: the element loop is **61%** of `ops::apply`, `Container::from_sorted` 1.9%, the whole container tail 7%. So the three-pass structure is not the problem and the kernel is — but what paid was **not running the loop**. Where intervals are few, the answer is each interval's span copied whole: two binary searches and an `extend_from_slice` per interval, instead of a test per element.

  ```text
  nruns    before     after   vs roaring   path
      1   3459 ns    307 ns        0.18x   seek
      2   3126 ns    335 ns        0.20x   seek
      8   3581 ns    619 ns        0.36x   seek
     16         -   1002 ns        0.58x   seek
     32         -   1774 ns        1.03x   seek
     64   3576 ns   3330 ns        1.89x   merge
   2048   4267 ns   4066 ns        2.36x   merge
  ```

  **11.3x at one interval**; `array x run` AND moves from 1.99x the reference to **0.18x**. The threshold is its own constant, `RUN_SEEK_RATIO = 64`, **because reusing `GALLOP_RATIO` would have shipped a regression**: at its value of 32 the seek path measures **6214 ns against the merge's 3634** — 1.7x worse — since its fixed cost falls per *interval* here and per *element* there. Found by forcing each path across the sweep rather than by reasoning about it.

**The high-`nruns` end closed 2026-09-07 by the mask-and-compact arm.** For each 8-lane block, only the intervals overlapping `[ v0, v7 ]` can matter — everything ending below `v0` was passed, everything starting above `v7` is out of reach — so OR-ing their range masks gives a **complete** membership mask, and the surviving lanes are shuffled to the front and stored in one go. That completeness is also why `AndNot` comes free: a lane missed by every interval that *could* contain it is genuinely outside the run. It would not be free if the interval scan were truncated for speed — a skipped interval would read as an absence.

  ```text
  nruns    before     after   vs roaring
     16         -    983 ns        0.57x
     32   1779 ns    981 ns        0.57x
     64   3330 ns   1061 ns        0.60x
    125   3337 ns   1049 ns        0.59x
    512   3494 ns   1406 ns        0.82x
   1024         -   1747 ns        1.04x
   2048   4066 ns   2466 ns        1.45x
  ```

**A branchless scalar compaction was tried first and is slightly *worse* than the plain loop** ( 2072 against 1887 ns at 64 intervals ), so the data-dependent branch was never the problem — vector width is the win. Recorded so it is not retried.

**And landing this invalidated `RUN_SEEK_RATIO`, which had to be re-tuned from 64 to 256.** A threshold is a claim about the alternative, so replacing the alternative rebuts it: 64 was correct against the scalar merge, the merge got ~3x faster, and the crossing point moved with it. At the stale value the seek path was taken at 32 intervals where compaction is **1.8x better**. Do not re-tune it against the scalar merge; that is no longer what runs.

Residual: **1.45x at 2048 intervals**, where a block spans ~4 intervals and the inner interval loop dominates the mask build. Everything from 1 to 512 intervals now beats the reference, and 1024 is at parity. `AndNot` deliberately stays on the merge — it keeps the values *between* intervals, a walk over gaps rather than spans, not this routine with a flag flipped. -- *source: M1 baseline; measured 2026-08-25, re-measured and largely closed 2026-09-07*

**Correction, 2026-08-26**: "all nine kind-pairs are specialized in both `ops::apply` and `ops::card`" was **false when written**. `ops::card` had arms for `bitmap x bitmap`, `run x run`, `bitmap x run`, `array x run` and `array x bitmap`, and **none for `array x array`** — the most common shape in a sparse posting-list workload fell through to the generic `Peekable<ContainerIter>` merge, which dispatches on container kind once per element on both sides. Flat **2.5x** over the same algorithm on `&[u16]`, m = 32 to 4096. Note what this is an instance of: the sentence immediately above is the warning that would have caught it, in the same item, written by the same hand. Fixed — `ops::array::and_cardinality` now exists and `card.rs` delegates to it, sharing `and`'s gallop-vs-merge decision. Measured −38% to −69%. Nine of nine is now true **for `and_cardinality`**; it is checked by `cardinality_identities_agree_for_every_kind_pair` in `tests/proptest_oracle.rs`, which pins the *answer* for every pair but cannot see a missing arm, since a slow arm and a fast one return the same number. -- *source: `intersect_crossover` in `benches/setops.rs`, 2026-08-26*

**Second correction, same day, and it is the larger one.** "Both `ops::apply` and `ops::card`" was reading `ops::card` as *`and_cardinality`*. The module holds two more entry points, and `is_disjoint` and `contains_all` had **one arm each** — bitmap x bitmap — with the other five kind-pairs falling through to per-value `Container::contains` dispatch. For a `Run` that is categorically wrong rather than slow: `iter()` enumerates every **value** of a container that exists to store **intervals**. `is_disjoint` on two run containers cost 124 us where the interval two-pointer costs 58 ns. The invariant being violated is simple enough to state as a rule for future arms: **`is_disjoint(a, b)` must never cost more than `and_cardinality(a, b) == 0`**, because it asks strictly less; it cost 7.7x more for arrays and ~1000x more for runs. Eight arms added, each the corresponding `and_cardinality` arm with the accumulator replaced by an early return. `OrdSet::is_subset` 1.69 ms → 98.3 us, moving from **8.7x slower than the `roaring` reference to 2.0x faster**. Covered by `both_predicates_agree_with_the_counting_oracle_on_every_ordered_pair` in `card.rs`, sabotage-checked in two directions. -- *source: `predicate_paths` in `benches/setops.rs`, 2026-08-26*

**The generalizable lesson, since this is twice in one day.** A missing arm is invisible to every correctness layer the crate has, because a slow arm and a fast arm return the same value — and it is invisible to review, because the code that is there looks reasonable and the code that is not there is not there. The only instrument that can see it is a benchmark that names the pair. Both gaps were found by *writing the benchmark for the arms nobody had benchmarked*, and in both cases the surrounding prose already asserted the arm existed.

**Third correction, 2026-08-28, and this one retracts two of this entry's own headline numbers.** "`run x run` cardinality is **35x faster** than the reference and `bitmap x run` **2.3x faster**" came from `cardinality_mixed`, whose run operand is `(0..30_000).map(|i| (ch << 16) | i)` — **one interval per chunk** — and whose `run_x_run` row passes *the same `OrdSet` on both sides*, so it measured eight interval comparisons against a self-intersection. `cardinality_mixed_distinct` fixes both and sweeps the interval count over the same eight chunks and the same `roaring::intersection_len`:

  ```text
    intervals/chunk      1        64      1024
    run x run        63x fast  1.08x   13.9x SLOW
    bitmap x run     4.3x fast 1.10x    8.5x SLOW
  ```

  So 35x was if anything *understated* at one interval. **But the "inversion" at 1024 is itself a fixture artifact and was retracted the next day** — `theirs()` never run-optimizes, so those rows compare our `Run` against roaring's `Bitmap`. With `roaring::optimize()` called on the same operands the ratio is **1.02-1.07x across 1 to 1024 intervals**, and roaring's own run x run at 1024 costs 17 206 ns against our 17 670. **A ratio between two systems that chose different representations measures the choice, not the code** — which is exactly what this correction was pointing out, and the replacement number reproduced the error one level up. See the retracted `run-x-run-inverts-at-high-interval-count`.

**And the arm that had existed longest was the one still broken.** The 2026-08-26 correction above added five missing `is_disjoint` / `contains_all` arms and left bitmap x bitmap alone because it was "already a word loop". It was — a **short-circuiting** one, which cannot be vectorized, and `--emit asm` showed **zero** vector instructions against `and_cardinality`'s full `cnt`/`uaddlp` ladder in the same object file. It cost **274.8 ns against the count's 167.6 — 1.64x to compute strictly less**, violating this crate's recorded invariant. Now 82.6 ns ( 3.3x, and 0.87x of the count ). Do not read "it is a word loop" as "it vectorizes". -- *2026-08-28*

- [ ] **array-intersect-simd-x86** ( *the AArch64 arm landed 2026-08-27; the x86_64 one is deliberately unwritten* ): **This entry over-claimed its own coverage until 2026-09-07, and that is why a performance bug survived eleven days.** It said `ops::array::simd` implemented the NEON 8x8 all-pairs merge for `and` / `and_cardinality` / `is_disjoint` / `contains_all`. Only **two** existed — `merge_and_into` and `merge_cardinality`. `is_disjoint` and `contains_all` were both plain scalar two-pointer merges, and because the backlog said otherwise nobody looked; `is_disjoint` was meanwhile losing to the count it is weaker than at every operand size ( see `array-is-disjoint-loses-to-its-own-count`, closed the same day ). A backlog entry that over-states what is done does not merely fail to prompt work — it **actively suppresses** the question. `is_disjoint` now has its arm. **So does `contains_all`, since 2026-09-07 — and this entry still said otherwise on 2026-09-12, which is the same failure it was corrected for five days earlier.** All four NEON arms now exist ( `merge_and_into`, `merge_cardinality`, `is_disjoint`, `contains_all` ). `contains_all` measured **2.2x across every size**, and against the count it is weaker than, 1.04x / 0.68x / 0.64x / 0.63x became **0.46x / 0.31x / 0.29x / 0.28x**; a false answer is **5.4 ns flat**, independent of `m`. Its two invariants — `found` accumulating across `a`-block advances, and `i` rewound on an early exit — are stated at the site and covered by a sabotage. See JOURNAL 2026-09-07. **What actually remains is only the x86_64 arm**, which is deliberately unwritten, and this entry should be read as being about that alone.

**`contains_all` landed 2026-09-07**, so the entry's claim is now true rather than aspirational — `merge_and_into`, `merge_cardinality`, `is_disjoint`, `contains_all`, all four with NEON arms. **2.2x across the board** ( 33.8 -> 15.3 ns at m = 64, 115.7 -> 53.1, 462.7 -> 206.5, 1790.5 -> 790.2 at m = 4000 ), and against the count it is weaker than it moves from 1.04x / 0.68x / 0.64x / 0.63x to **0.46x / 0.31x / 0.29x / 0.28x**. The false answer is **5.4 ns flat, independent of `m`**. It needed a different block rule from `is_disjoint`, not the same one inverted: containment is asymmetric, so one block of `b` is decided by *several* blocks of `a` and matches must accumulate across `a`-advances; and the scalar tail is only sound if `i` is rewound to a point below every undecided `b` value. Both invariants are stated at the site and both are covered by a sabotage that goes red. Original note: unlike `is_disjoint` it was **not** a discipline violation — measured against the count it is weaker than it sits at 1.04x / 0.68x / 0.64x / 0.63x for m = 64 / 256 / 1024 / 4000, so only the smallest size is marginal. The case for vectorizing it is speed alone ( the scalar merge is ~1790 ns at m = 4000 ), not a broken invariant. It is also the **harder** of the two: `contains_all` is asymmetric — `b` drives, and probing `b` into `a` is the only legal direction — so the block rule has to decide *when a block of `b` is fully decided by the current block of `a`*, which is only when `bmax <= amax`. Do not copy `is_disjoint`'s loop across without settling that.

  The x86_64 arm, for the two that do exist: measured at **1.8x / 3.8x / 4.8x / 6.0x** at m = 256 / 512 / 1024 / 4096. The x86_64 SSE4.1 `_mm_cmpestrm` arm — the natural equivalent, and what the published SIMD-intersection work uses — is **not** written, because there is no x86_64 machine here to measure it on and an unmeasured kernel would put a number in the module's table that nobody has seen. Every non-AArch64 target correctly runs the scalar fallback, so this is a missing optimization and not a correctness gap.

CI runners are `x86_64-unknown-linux-gnu` while everything here was measured on `aarch64` — see `ci-has-never-run`, which records the same split. That makes CI the natural place to get the first honest x86 number. Do not port the NEON arm by translating intrinsics and quoting the AArch64 speedups; `_mm_cmpestrm` has different throughput characteristics and the crossover against the gallop path must be re-measured, not assumed.

The gallop path is **untouched on purpose** and must stay that way: a vector merge measured **0.84x** past `GALLOP_RATIO`, because vector width does not change an asymptote. -- *source: 2026-08-27*

- [ ] **kind-aware-temporary-promotion** ( *narrowed 2026-08-26; **inputs re-measured 2026-09-07 — the conclusion stands, one number has moved 3x*** ):

**Resident `m*` holds.** Re-run of `intersect_crossover`: array beats bitmap at m = 64 ( 47.3 vs 94.1 ns ) and loses at m = 128 ( 108.9 vs 94.1 ), interpolating to **m\* ≈ 113** against the recorded ~118. Bitmap is still flat across the whole range, as recorded — though at **94 ns, not the 167 ns** written here, so quote the flatness and not the absolute.

**Streamed `m*` has moved from ~80 to ~260**, and the cause is identifiable: this entry was measured **2026-08-26**, and the NEON array arm landed **2026-08-27**, the day after. A faster array kernel keeps arrays competitive further up, so the crossover rises. `intersect_crossover_streamed`, K = 512 pairs:

  ```text
  m      array      bitmap    winner
   144   67.9 us    125.6 us  array
  1024  497.4 us    125.1 us  bitmap
  4096  2007.9 us   126.5 us  bitmap, 16x
  ```

**The direction strengthens the verdict rather than upsetting it.** A higher streamed `m*` means an array stays the better representation over a wider range, so promotion is licensed *less* often, not more — the entry's "`m*` alone does not license promotion" and its narrowing to an n-ary rewrite both survive. Do not quote ~80; and do not re-derive the promotion economics from it, since every `k >= …` threshold here was computed against that number.

  Original entry: the array/bitmap intersection **time** crossover `m*` is ~118 resident and ~80 streamed, against `ARRAY_MAX` = 4096 — 35-51x below the *space* threshold, confirming that `ARRAY_MAX` is not a time constant and was never derived as one. Bitmap x bitmap is flat at 167 ns resident / 275 ns streamed across the whole legal cardinality range, density-independent. But `m*` alone does **not** license promotion: a container that should be a bitmap is not one, and making it one costs `189 ns + 0.47 ns/element`. Charged honestly for a single intersection with both operands promoted, promotion loses 3x at m = 256, loses at m = 1024, and ties within noise at m = 4096. It pays only when the same operand is intersected k times — k >= 2 at m >~ 1024, k > 7 at m = 256, k > 150 at m = 144. So the item that remains is an **n-ary intersection rewrite** that promotes an operand meeting many others, not a per-pair kernel decision, and it should be evaluated against `ops::nary` rather than against `ops::apply`. -- *source: JOURNAL 2026-08-26*

- [ ] **bit-sliced-lens-proposed-by-a-consumer**: a downstream project ( `haiiie`, binary-code search over yesno ) has filed a prescription for a **fourth lens**: `slice`, reading a *series* of sets as one integer per ordinal ( `value( o ) = Σ 2^j · [ o ∈ planes[j] ] `). **The document is no longer in this tree.** It was filed at `.agents/docs/PROPOSALS/bit-sliced-lens.md` and moved out on 2026-09-13, so **this entry and the audit addenda below are the surviving record and are written to stand alone** -- every one of the six corrections is reproduced here in full rather than left as a pointer. The corrected document still exists outside the repository, and a copy carrying all six corrections is recoverable from git's index ( `git show :.agents/docs/PROPOSALS/bit-sliced-lens.md` ) for as long as that index entry lives; treat neither as archival. Four parts: `ops::csa` ( the three-input, two-output member of the container kernel family ); `Slice` with `add_many` ( per-ordinal popcount across `n` sets, Harley-Seal, a derived `5n` word-ops per lane against ripple's `2nL` -- **the proposal said `3nL`; see correction 3 below, and do not quote the original pair** ); `ge` / `top_k` by descending the planes on cardinality alone; and `OrdSet::view_count`.

**Three things make it worth reading rather than filing.** It is the **transpose of `bignum`** — that lens reads one set as many integers, significance inner; this reads many sets as one integer per ordinal, significance across sets — so it is the same reinterpretation doctrine and adds no storage format. `formal-model.md:2071` already names O'Neil and Quass's bit-sliced indexes as sitting on the axis this crate deliberately left open, and the proposal is careful not to disturb that: a slice is still `n` equality-encoded keys. And **`view_fold` already computes the count and discards it** — `fold.rs:57` says "the walk counts, and each variant reads its answer off the count", then `Reduce::keep` reduces it to a `bool`.

**It explicitly declines to add `Reduce::Count`**, on the grounds that `fold.rs:53` closes that enum at three because a fold's codomain is a set, and a count changes the codomain. That is the right reading of the comment, and it is the detail that suggests the proposal was written against the code rather than against the README.

**The part to be skeptical of is the motivation, not the mechanism.** The mechanism is plainly set algebra. But the crate has a standing product boundary ( `LTM/context-eligibility-product-direction.md` ) that scopes out similarity search, and this arrives from a vector-search consumer. The proposal's own defence is that a caller ranking a cohort by a stored numeric attribute gets exactly as much from it — which is testable: **if `top_k` cannot be motivated from the faceted-search use case in `README.md` without mentioning vectors, it does not belong here.** It probably can ( "top ten by rating within this filter" is the same call ), and that is the check to run before any of it is written.

**Superseded 2026-09-14: the evidence arrived.** This paragraph said the benchmark "does not exist yet, and is not imminent", on the strength of `../haiiie` being an empty directory with zero files when checked on 2026-09-13. **It now holds 6 696 files and the measurement landed the next day** ( see below ). The project went from not-started to delivering the measurement in about a day, so the inference from an empty directory to "not imminent" was wrong by an order of magnitude in the only dimension that mattered. Recorded rather than deleted because the reasoning was sound and the conclusion was still wrong: a directory listing dates a project's *start*, not its pace, and nothing about the first licenses a claim about the second. This does not change the verdict, and it is the reason the §7 gaps were worth doing separately: they were explicitly "worth fixing whether or not §1-§5 land", and all four are now done while §1-§5 remain correctly untouched.

**THE BENCHMARK LANDED 2026-09-14, AND IT SHRINKS THE PROPOSAL'S CASE RATHER THAN CONFIRMING IT.** `haiiie` produced the measurement this entry was waiting on. **Frame, because it is the whole point**: word operations over one 65 536-ordinal block, in `haiiie`'s own `[u64; 1024]` plane buffers. These are **not** yesno container operations and must not be quoted as such.

```text
  addends  levels    ripple   carry-save   ratio   2L/5 predicted
       32       6   25.0 us      22.9 us   1.09x           2.4x
      128       8  114.1 us      90.8 us   1.26x           3.2x
      512      10  574.3 us     379.0 us   1.52x           4.0x
```

**The corrected model over-predicts by two to three times.** Correction 3 below replaced the proposal's `3nL` with `2nL` and derived `2L/5` -- 3.2x at `L = 8`, 4.0x at `L = 10`. Measured: **1.26x and 1.52x**. So correction 3 was right that the original pair was inflated and still wrong as a *time* predictor, because a word-operation count says nothing about memory traffic, vectorization, or the `O(L^2)` fold carry-save pays once per block. The advantage is real and does grow with `L` as the model says it should; the magnitude is not the model's.

**This vindicates the parenthetical rather than the arithmetic, and that is the durable part.** Correction 3 already warned that "at container granularity the word-operation count is the wrong model outright" and that `5n` against `2nL` is "a lane-level sanity check, not the case". The measurement says the sanity check fails even at lane level, on the reporter's own buffers, where the model should have been at its most accurate. **Do not repair this by fitting a third constant.** The lesson is that the family of models is wrong for the quantity, which is the same conclusion `cost-model-cannot-see-materialization` reaches from the other end.

**The reason the ratio is 1.5x and not 5.5x is a bug the benchmark found in its own baseline, and this is the most reusable thing in the report.** The first run had an arm that allocates two vectors per level per addend -- included only to show what a container-level operator would cost -- **beating ripple** at 128 and 512 addends. Allocation cannot explain that; loop order can. The ripple baseline was **word-major with a per-word early exit on an empty carry**, and at density 0.5 that branch almost never fires, so it bought nothing and blocked the level from vectorizing. Rewritten **level-major** with the exit checked once per level: **348 us -> 114 us at 128 addends, 3.1x, in production code.**

  Before that fix the ratios were **3.6x at 128 and 5.5x at 512** -- comfortably above the corrected model and sitting right where the proposal's original inflated `3L` estimate predicted. **Those numbers would have read as confirmation of a model this entry had already corrected.** A naive baseline does not announce itself; it flatters whatever it is compared against, and it flatters it most convincingly when the wrong answer is the one you expected. If `ops::csa` is ever built, benchmark it against a **level-major** ripple, not the natural-reading one.

**What this does to the recommendation.** §4 calls `add_many` "the operation worth the proposal"; on this evidence that is too strong, and the reporter says so unprompted. The better case is the one §2 and §3 already make and §4 does not reuse: **`view_count`, which `view_fold` computes and discards today** ( `fold.rs:57` ), and the `ge` / `top_k` descent on cardinality alone. A 1.3x kernel does not carry a four-part proposal; a count already being thrown away does. Re-pitch on that, or do not re-pitch.

**Also reported, and it closes a question this entry did not know it had**: `Snapshot::key_stream` is now what `haiiie`'s `load_block` uses -- seek plus one `next_chunk` rather than materializing a whole key per block per dimension -- so the lazy leaf has its first external consumer doing exactly what it was built for. And the counter-array kernel the proposal wanted at M3 was **not** built, deliberately: `load_block` returns a dense mask, so a scatter kernel would densify and then scatter, which is strictly worse. Its premise is a sparse read path returning positions rather than bits, and it waits for one. -- *source: `haiiie` benchmark report, 2026-09-14*

**CORRECTED LATER THE SAME DAY, TOWARD THE ORIGINAL CLAIM, AND THE VERDICT SURVIVES ANYWAY.** The microbenchmark above was right and an intervening end-to-end null result was wrong. Sequence, because the sequence is the lesson: the consumer's first end-to-end profile showed carry-save delivering **nothing** ( 3.51 ms against ripple's 3.41, marginally slower, noise ) and they were about to record that as the final word. It was **masked by read cost**. After fixing their read path -- see below -- carry-save measures **1.66 ms against 2.22 ms, 1.34x**, which matches the microbenchmark's 1.26x at that query width almost exactly.

  **"Measured no effect" meant "not yet", not "never".** Something else dominated, and the benefit was real the whole time. That is the mirror of the baseline error recorded above: there a naive comparand *flattered* the change, here an unrelated bottleneck *hid* it, and in both cases the end-to-end number was the misleading one while the microbenchmark was right. Neither direction is detectable from the number alone.

  **It does not revive §4.** 1.34x end to end is still not an argument for adding `ops::csa` to this crate, and `view_count` is still the part that stands on a fact rather than a ratio. Recorded because a future session finding "the consumer measured no end-to-end gain" in this entry would otherwise have the wrong reason for the right conclusion.

**AND `bitmap_words` -- an API this crate added for this consumer -- is finally consumed, at 4.2x on their dominant cost.** Their read path was building an Arrow mask and reassembling 1 024 `u64`s from its bytes, once per block per dimension; `bitmap_words` lends them directly. **Posting-list reads 2.02 ms -> 0.48 ms, whole scan 3.51 ms -> 1.66 ms**, at 262 144 documents, D = 256, a 118-bit query, over a real store. Sparse containers take a position scatter instead, which beats materializing a mask to copy out of.

  **Its two `None` causes earned the doc comment they were given.** `None` means *not a bitmap* **or** *a `Shared` buffer whose alignment `try_cast_slice` refuses*, and the consumer reports that documenting the distinction is why they wrote one arm rather than assuming `None` meant array-or-run. Both take the scatter, so it did not change their correctness -- but the comment is what stopped them inferring a wrong cause. A gap this crate filled on request, then watched go uncalled for two milestones while the thing it was for stayed the bottleneck; worth knowing before treating "we built what they asked for" as evidence a request was well-founded.

**AND THE PROPOSER WITHDREW THREE OF THE FOUR PARTS THE SAME DAY**, which changes what this entry is holding. The consumer has re-scoped its own milestone to **`OrdSet::view_count` alone**. `ops::csa` and `Slice` / `add_many` are the parts the new evidence is **against**; `ge` / `top_k` is to be argued separately on its own merits or not at all. **The suggested implementation order recorded below is therefore obsolete as a plan** -- it sequences four parts, of which one remains asked for. Read it as a record of how the parts depend on each other, not as a queue.

**`view_count` survives on an argument that has no ratio in it, and that is why it survives.** `view_fold` already computes the count and discards it ( `fold.rs:57`: "the walk counts, and each variant reads its answer off the count", then `Reduce::keep` reduces it to a `bool` ). So it is a capability being **thrown away**, not a kernel being sped up: no new lens, no `ops::csa`, no commitment to wiring a lens into lazy evaluation, and **nothing a benchmark can refute**, because no measurement is load-bearing in it. Everything the CSA numbers took away was resting on a ratio.

**The structural lesson is worth more than this proposal.** Four parts shared one argument, so when that argument failed under measurement, all four fell together -- including the one that never needed it. A proposal whose parts each carry their own case loses only the part that was wrong. Filing several changes under one justification is efficient to write and total-loss to refute, and the tell is available before any measurement exists: ask which parts survive if the headline number comes back at 1x. Here the answer was one, and it was knowable on the day the proposal was filed. -- *source: consumer re-scope, 2026-09-14*

**Do not implement from the proposal.** It asks to be held until `haiiie` produces the benchmark — fused CSA against both ripple-carry and the composed-`ops` construction — since every ratio in it is *derived, not measured*, and it says so. The composed-vs-fused claim in particular is about passes and intermediates ( five container walks and three throwaway containers, against one walk and none ), not about word-op count, which is the same five either way. Suggested order if it lands: the four API gaps below, then `ops::csa` with a generic oracle arm written first ( no Roaring library exposes a three-input kernel, so the generic arm carries the whole oracle burden ), then `Slice`, then `top_k`, then `view_count`.

**Four smaller gaps ride along and are worth fixing regardless**, all raised by the same review. **`Snapshot::key_stream` was implemented on 2026-09-13** and is struck from this list; the remaining three stand. `unstable_arrow::bitmap_words` returning `Option<&[u64]>` so a caller can decline the 8 KiB re-encode that `bitmap_mask` performs for a memtable-resident `BitStore::Mut` ( the false "without copying" claim in that function's own doc was corrected the same day, so the behaviour is at least documented now ); ~~`Snapshot::key_stream( key )`, which is what would finally make `Backing::Paged` reachable~~ — its own doc admits no leaf reports it because `Snapshot::load` materializes first, and a `10^9`-ordinal key is ~122 MB a chunk-at-a-time consumer never needed ( **the proposal's figure, and it is wrong** -- see the correction below ); `WriteBatch::merge_set`, since `store_set` and per-ordinal `insert` leave nothing in between and bulk ingest into an existing key becomes a read-modify-write outside the batch; and `Counts::row( r ) -> &[u32]`, since `Counts::get` asserts per cell and `v` is private. -- *source: external design review for `haiiie`, 2026-09-13*

**DONE 2026-09-13 -- `Snapshot::key_stream`.** `yesno-core/src/db/keystream.rs`. One index range scan resolves every visible chunk to a `( ChunkKey, ChunkRef )` and merges the memtable over it; payloads decode one at a time in `next_chunk` with the store lock taken and released per chunk, and each step carries its cardinality so `next_cardinality`, `cardinality_dyn` and `cardinality_hint` read **no payload at all** -- the same `card_m1` property `Snapshot::cardinality` has. **Measured** at 500 chunks x 8 ordinals, reopened so the read comes off disk: `load` 1 631 allocations, opening the stream plus one chunk **49**, counting **47**. Pinned by `a_key_stream_does_not_decode_the_key_it_streams` in `tests/allocation.rs`, which is the only layer that can see the difference -- a stream that resolved every chunk and then decoded them all anyway returns identical values.

Three things a later session should not have to re-derive. **The memtable overlay is still collected eagerly**, and that is a lock-order requirement rather than an oversight: `merged_chunks`'s own comment records that a merge streaming from both the memtable and the store live would be the first thing in the crate to impose an order between those two locks. The overlay is bounded by the flush threshold and the per-chunk work this exists to avoid is on the disk side, so the laziness is where it needs to be. ( This sentence said "the 122 MB"; see the correction below -- `read_container` is zero-copy and the cost is allocations, not resident bytes. ) **Liveness is checked per chunk, not once**, because a stream reads over an unbounded window and a snapshot can be evicted inside it -- after eviction the floor no longer holds this reader's extents and a captured `ChunkRef` may point at reused space. **The stream holds the reader slot itself**, so it may outlive the `Snapshot` value it came from. Sabotaging each of those three fails exactly one test and no others; all five sabotages were run.

It also **diverges from `load` deliberately on one point**: `merged_chunks` silently drops a chunk whose payload fails to decode, so `load` answers a corrupt key with a short set, where this propagates the error. A short answer presented as a complete one is the worse failure, and `next_chunk` already returns a `Result`.

`Backing::Paged` is no longer aspirational and **its doc comment was corrected in the same change** -- it had said "No leaf reports this yet" since the planner was written. That is the same stale-rationale failure as `bitmap_mask`'s "without copying", found twice in one session in two unrelated modules, which is worth noticing as a pattern rather than as two incidents.

**AUDIT 2026-09-13 — every citation in the proposal was checked against the tree and they hold**, including the two long quotations ( `formal-model.md:2071-2077`, `bignum/mod.rs:1-8` ), the `Reduce::keep` body, `Backing::Paged`'s "no leaf reports this yet" doc, and all four of the §7 gaps. The `View::interleaved( L )` plus `IntLayout::dense( L )` denotability claim in §1.3 also checks out. **Five corrections follow. They do not change the verdict above -- accept the framing, hold the implementation -- but four of them must be applied before any of it is written, and the fifth is a one-line fix worth doing now.**

1. **`Slice::sub` must wrap, not saturate, and the proposal's appeal to precedent is wrong twice.** §4 gives the signature as "saturating at zero, as bignum does". `bignum/addsub.rs:12` says flatly "**There is no saturating arithmetic and there will not be**", and `BigUint::sub` returns `Option<BigUint>`, refusing on underflow rather than clamping. The decisive argument in that module header transposes onto a slice exactly: the reader cannot saturate, because reading integer `k` at `width_bits = W` gathers only `[k*stride, k*stride + W)` and is therefore `x mod 2^W` **by construction**. A slice's `value_at( o )` reads `L` planes and is `mod 2^L` for the same reason, so truncation is again the only write rule that agrees with the read rule. A slice cannot borrow `bignum`'s `Option` refusal either, since subtraction is elementwise over `N` ordinals at once -- the honest signature wraps and says so, or returns the borrow-out plane as a mask. **This is the one place the proposal contradicts the doctrine it otherwise honours, and its own §1.3 transpose argument is the tool that catches it.**

2. **`ops::csa` must return `( Option<Container>, Option<Container> )`.** Every shipped kernel -- `ops::{and, or, xor, and_not}` over `ops::apply` -- returns `Option<Container>`, with `None` meaning the empty result. The proposal's `-> ( Container, Container )` would make `csa` the only member of the family that materializes an empty container. This is not cosmetic: in `add_many` over sparse addends a `None` carry **is** the early exit that §4's second bullet spends a paragraph on, so putting it in the return type makes that guard free instead of "close to dead code at chunk width".

3. **The §4 speed ratios are inflated roughly 1.5x by asymmetric operation counting, and the correct numbers should go into the benchmark as the expectation.** Fused CSA at 5 word operations is right ( `t = a^b`, `sum = t^c`, `a&b`, `c&t`, `or` ). But the "obvious ripple-carry" baseline for adding a **one-bit** addend into an `L`-plane accumulator is a half-adder ripple -- `new_carry = acc[j] & carry; acc[j] ^= carry` -- which is **2 operations per plane, so `2nL`, not `3nL`**. The ratio is therefore `2L/5 = 0.4L`: **3.2x at `L = 8` and 4.0x at `L = 10`**, not the 4.8x and 6.0x quoted. The win is still real and still worth the kernel. Carrying the inflated pair into `haiiie`'s benchmark is how an honest result gets read as a regression when it lands. Note also that §2 already has the better argument and §4 does not reuse it: at container granularity the word-operation count is the wrong model outright, because allocation, cardinality counting, kind dispatch and the demotion check dominate. **Five passes and three throwaway containers against one pass and none is the claim to benchmark; `5n` against `2nL` is a lane-level sanity check, not the case.**

4. **§1.2's "the encoding stays where the document put it" is doing more work than it admits, and amending `formal-model.md` is an unlisted cost.** A caller bit-slicing a rating column into four planes is performing O'Neil-Quass bit-slice encoding. The proposal's dodge -- that yesno does not do the encoding, the caller assigns the keys, and each key is still one equality-encoded set -- is honest, but the consequence is that yesno would ship the **arithmetic** for an encoding it declines to own, while `formal-model.md:2071-2077` currently reads as though yesno has nothing on that axis at all. That paragraph would need an amendment in the same change, and §8's sequencing does not list it.

5. **The space consequence of a slice is analysed in §4 and then not drawn.** The early-exit bullet correctly observes that at density ~ 0.5 no carry plane is empty below `j` ~ 16. The unstated corollary is that those planes are bitmap containers throughout, so a slice over a `10^9`-ordinal universe at `L = 10` is ~1.25 GB -- and `add_many` over `10^9` ordinals is the headline claim. For the faceted-search caller the addends are sparse and this never bites. Both regimes should be named rather than one.

**A separate defect found while checking §7 gap 1, and worth fixing whether or not any of this lands.** `unstable_arrow::bitmap_mask`'s own doc comment promises the container's bits "**without copying**". That is false for a memtable-resident `BitStore::Mut`, where `to_boolean_buffer` ( `buffer.rs:232` ) re-encodes 8 KiB byte by byte. The proposal files the behaviour as a missing API; the wrong doc comment is a one-line fix that stands on its own. If `bitmap_words` does land, its doc must distinguish the **two** causes of `None` -- not a bitmap, versus a `Shared` buffer that `bytemuck::try_cast_slice` refuses for 8-byte misalignment -- or a caller will read `None` as "not a bitmap" and be wrong on any mmap'd container at an odd offset. -- *source: audit of the proposal against the tree, 2026-09-13*

**AUDIT ADDENDUM 2026-09-13 -- a sixth correction, and the largest: `slice` would be the first lens ever wired into the expression surface.** Raised by a reader's question, not by the audit. **`Expr` and `ChunkStream` appear zero times in `bignum/`, `matrix/`, `view/` and `pack/`** -- all three shipped lenses are strictly eager, taking an `OrdSet` and returning an `OrdSet`, a `BigUint`, a `BitMatrix` or a scalar, and none has ever touched the lazy surface. §5's `ge( t ) -> Expr` and `top_k( within: &Expr, k )` are therefore **not** covered by §1.3's "it is the transpose of a lens that already ships": the transpose argument licenses the *arithmetic* and says nothing about API shape. Symmetry with `bignum` would give `ge( t ) -> OrdSet`.

The asymmetry is probably **right** -- `top_k`'s entire economy is `L` cardinality evaluations materializing nothing, and an eager `ge` would materialize a set per plane, which is the cost the operation exists to avoid -- but it should be argued rather than smuggled. It makes the filing two proposals: bit-sliced arithmetic over sets, which fits the existing eager doctrine, and **the first wiring of a lens into lazy evaluation**, which is a much larger architectural commitment currently riding on the first one's argument. Three consequences: the decision actually lands at §8's **step 3** ( `ge` ), not step 4 ( `top_k` ) where the payoff is; **`ge` needs no new `Expr` variant** and so is far cheaper than it looks, because a descending compare-with-borrow is a boolean formula over the plane sets and lowers to the existing `And` / `Or` / `AndNot` / `Not` over `Expr::Set` leaves -- **verify that before writing anything**, since a `ge` needing a new variant is a different proposal again ( `Expr` is matched exhaustively in `open_planned`, `flatten_or` and the planner ); and `top_k` re-opens its expression `L` times with a tied-set tree that deepens per plane, so materializing `tied` as an `OrdSet` each step is the obvious alternative and should be measured rather than assumed away. `view_count` ( §3 ) is unaffected and stays eager, consistently with `view_fold`.

**DONE 2026-09-13 -- the three remaining §7 gaps, all of them.**

* **`unstable_arrow::bitmap_words( &Container ) -> Option<&[u64]>`.** One line over `bits().try_words()`, as the proposal said. The doc carries the part the proposal did not: **`None` has two causes** -- not a bitmap, or a bitmap whose mmap'd payload is not 8-byte aligned and so cannot be viewed as `&[u64]`. A caller reading `None` as "not a bitmap" and unwrapping will panic on a healthy store-backed container at an odd offset. Pinned by `lending_words_implies_a_mask_but_not_the_other_way_round`.
* **`Counts::row( r ) -> &[u32]`**, plus `rows_iter()`. One bounds check per row instead of one per cell. `argmax_in_row` was the in-tree instance of exactly the complaint and now reads through `row`. Pinned differentially against `get` on a **non-square** fixture, which is what catches a `rows`/`cols` swap.
* **`WriteBatch::merge_set( key, &OrdSet )`.** Unions a set into a key inside the batch, closing the read-modify-write that had to happen outside it. **It does not lower to `Op::PutChunk`, and the reason is the finding below.** It folds maximal runs exactly as `Db::insert_many` does -- a contiguous span becomes one 32-byte `SetRange` whatever its width, the rest becomes scattered values that `plan_ops` coalesces per chunk -- and streams straight off `set.iter()`, which is already sorted and deduplicated, so unlike `insert_many` it needs no intermediate copy.

**FINDING 2026-09-13 -- `Op::PutChunk` replaces when it commits and unions when it replays, and one op is all that hides it.** Reached while implementing `merge_set`, which was going to be "`store_set` without the leading `DeleteKey`" until the tests said otherwise.

* **Live**: `Op::PutChunk` applies through `Memtable::put_chunk`, which **overwrites** the chunk's MVCC value.
* **WAL**: it logs as a bare `RecType::ChunkImage` -- key, then the chunk's ordinals, **no delete**.
* **Replay**: `apply_record` applies `ChunkImage` with a per-ordinal `insert`, which **unions** into whatever is underneath.

The two agree today only because the single producer, `WriteBatch::store_set`, emits `Op::DeleteKey` first, so both are unioning into an emptied key. **Drop that one op and a commit and its recovery differ** -- silently, and only after a crash. Demonstrated: removing the `DeleteKey` makes the committed state `[3, 4, 65536]` and the replayed state `[3, 4, 65536, 458752]`, an ordinal the commit had deleted returning from the log.

No test covered it, because every existing test either checkpoints before reopening or never reopens, and unioning into an *empty* key is indistinguishable from replacing it -- which is why the hole stayed open. `store_set_replays_to_what_it_committed` now pins it: it populates the key first, with ordinals the replacement does not contain, and fails on the sabotage.

`Op::to_record`'s own doc comment said `PutChunk` "logs as a **delete plus the chunk's ordinals**" -- describing the delete that would have made the two paths agree, and that is not in the code. That is the **third** wrong comment found this session, after `bitmap_mask`'s "without copying" and `Backing::Paged`'s "no leaf reports this yet", and the first one where the wrong comment was actively concealing a defect rather than merely being out of date. Both it and `apply.rs`'s `ChunkImage` comment ( which named `store_chunk`, a function that exists nowhere in the tree ) now state the divergence and warn against adding a `PutChunk` producer that omits the delete.

- [x] **wire-a-paged-leaf-into-the-expression-surface** ( raised and **DONE 2026-09-13**, immediately after `Snapshot::key_stream` landed ) `KeyStream` exists and is the crate's only producer of `Backing::Paged`, but **nothing can put it in an expression**, so every consumer still goes through `Snapshot::load`. `yesno-flight/src/expr.rs:47` lowers `SetExpr::Key( k )` to `Expr::set( snap.load( *k )? )`, and `lib.rs:597` does the same for the bare-key ticket path.

**Check the cost claim before selling this, because the obvious one is wrong.** `ShardStore::read_container` is zero-copy -- a store-backed container aliases the mapping -- so `load` does **not** read a key's bytes into memory and the pages fault in only as touched. What it spends is a container built per chunk, about **three allocations each**: measured 1 631 allocations for a 500-chunk key against 49 for opening a `key_stream` and taking one chunk. At ~15 000 chunks that is tens of thousands of allocations before evaluation starts, and an `And` that will skip most of the key pays every one of them. That is the real argument; "it avoids reading 122 MB" is not, and the §7 gap entry that said so has been corrected.

**The second payoff is the planner, and it may be the larger one.** `Backing::Paged` exists so a plan does not drive from an operand that faults per chunk, but **every leaf in production reports `Memory`**, so `chunk_cost` returns 2 for everything and the backing-aware branch is inert outside its unit test. A paged leaf is what switches it on.

**The blocker is real and is not "add an enum variant".** `Expr::Set( Arc<OrdSet> )` is the only leaf shape; `Expr` is `Clone + Debug` while a `KeyStream` is a stateful non-clonable cursor, so a stream cannot live in an `Expr` -- it needs a **re-openable factory**. And `stream/` currently has **zero** references to `db/`: the dependency is one-way, `db` -> `stream`, which `db/keystream.rs` follows. An `Expr::Key( Arc<Snapshot>, u64 )` would invert that layering.

The shape that avoids all of it: declare a factory trait **in `stream`** ( `Debug + Send + Sync`, one `open( &self ) -> BoxedStream` ), add one `Expr` variant holding `Arc<dyn ThatTrait>`, and implement it **in `db`** for a `( Snapshot, key )` pair. The dependency stays one-way, `Expr` stays `Clone + Debug`, and a failed open returns an error-yielding stream so `open_planned( &self ) -> BoxedStream` keeps its signature rather than growing a `Result`. Note this is the same seam §5 of the bit-sliced proposal needs, so the two should be designed together rather than twice -- see `AUDIT ADDENDUM 2026-09-13`.

**DONE 2026-09-13, as the factory shape above.** `stream::ChunkSource` ( `Debug + Send + Sync`, with `open` plus the three leaf statistics and `backing` ), one new `Expr::Source( Arc<dyn ChunkSource> )`, `db::keystream::KeySource` implementing it over a `( Snapshot, key )` pair, and `Snapshot::key_expr( key ) -> Expr` as the entry point. `yesno-flight`'s `lower` now lowers `SetExpr::Key` to `key_expr` instead of `Expr::set( load( k ) )`. The layering stayed one-way -- `stream` declares the trait, `db` implements it -- and `open_planned`'s signature is unchanged, with a failed open returning the new `stream::ErrStream` rather than an `EmptyStream`, so an evicted snapshot reports instead of reading empty.

**Measured payoff**, on a 600-chunk key intersected with a 5-chunk one, reopened so the read comes off disk: **1 994 allocations eager against 169 lazy, an 11.8x reduction**. Pinned by `a_lazy_leaf_does_not_decode_what_the_operator_skips` in `tests/allocation.rs`, which is the only layer that can see it -- both forms return the same set, so nothing else distinguishes "skipped the chunk" from "decoded it and threw it away". The lazy figure is deliberately not near zero: opening still resolves all 600 chunks to references.

**Six planner sites had to be handled, and the compiler only caught four.** `cheap_yield`, `yield_chunks`, `cardinality_cost` and `open_planned` are exhaustive and failed to build. `bounds` / `bounds_from` also failed and take the source's prefix span widened to ordinals, which is safe because that function documents wider as the safe direction. The two the compiler did **not** catch were the dangerous ones: `same_shape` would have compared a source unequal to itself, so any expression containing one would run all `MAX_PASSES` instead of converging; and `occupancy_of` falls through to `None` **deliberately**, because feeding a prefix *span* to `PrefixOccupancy::from_range` would claim every prefix between the ends is occupied, and unlike `bounds` an overstatement there picks a worse segmentation rather than merely forgoing one. `cheap_to_reopen` excludes `Expr::Source`, measured 2026-09-13 and then **re-measured the same day after the reason changed**. Reopening a source was `O( chunks )` ( ~35 us at 1 250 chunks/key ) only because `open` rebuilt the plan; `KeySource` now shares one immutable `Plan` behind an `Arc`, so a reopen is **~61 ns and flat in chunk count**, within ~1.4x of a resident leaf's `Arc` bump. **Do not quote the old ~0.8 / 3.8 / 35 us figures.** What still excludes it is `occupancy_of` returning `None` for a source, which makes `compute_segments` decline regardless; including it now costs only **+6.0% / +3.9% / +1.2%** allocations on an 8-way `Or` at 100 / 1 000 / 10 000 chunks ( was +38 / +43 / +48% ), still strictly worse but no longer by much. **DONE 2026-09-13**: `ChunkSource::occupancy` is implemented ( `KeySource` builds it exactly, from the prefixes its plan already holds ), `Expr::Source` is in `cheap_to_reopen`, and segmentation over lazy leaves runs for the first time. It is gated on size because the setup is a flat **+128 allocations per evaluation** while the benefit grows with the merge avoided: **2.0-3.0x slower** at 11-44 chunks per operand, break-even at ~88, then **1.11x / 1.17x / 1.26x / 1.32x faster** at 110 / 165 / 220 / 330. `SEGMENT_MIN_CHUNKS` = 128 sits above break-even with margin, and the gate lives in `occupancy_of` rather than `cheap_to_reopen` so that predicate keeps answering only what its name asks. A resident `Expr::Set` operand is deliberately not gated -- the number was measured for sources. Full tables in JOURNAL 2026-09-13.

`stream/mod.rs`'s standing comment -- "that works only because every leaf today happens to be a materialized `OrdSet`" -- was the assumption this change invalidates, and was rewritten in the same change to say why `ChunkSource` carries statistics at all.

- [x] **decline-segmentation-before-opening-operands** ( raised and **DONE 2026-09-13** ) `SEGMENT_MIN_CHUNKS` = 128 gates segmentation in `occupancy_of`, which is **after** `compute_segments` has already opened every operand to collect prefix spans. For an operand that is obviously too small, that work is thrown away.

**The severity is not established, and the claim that motivated this item was withdrawn.** It was filed on a report that a `haiiie` operand is ~1 chunk by construction, two orders of magnitude below break-even. That session retracted it the same day: the one-chunk figure describes its *gang cursor*, which loads `OrdSet`s directly and **never builds an `Expr` at all**, so it never reaches `compute_segments`. The only expression it hands yesno is a filter of **2-5 operands, each spanning many chunks** -- above the gate rather than below it. Treat neither figure as evidence here; **no caller has been verified on either side of the threshold.**

**What stands without any caller** is that the decline happens later than it needs to. Adding `Expr::Source` to `cheap_to_reopen` moved it: a union of sources used to decline at that first gate and now passes it, opens every operand for spans, runs the span pre-check, and only then declines at `occupancy_of`. Measured at `k` = 8 that is a flat **+11 allocations** per evaluation. **Re-derive the severity before pricing this**; the earlier "~100 opens per evaluation" model came from the retracted claim and there is no measured width to replace it with.

The fix is a short-circuit, not a new policy, and it is worth doing on the wasted-work argument alone: in `compute_segments`, **before** collecting spans, decline if any operand is an `Expr::Source` whose `chunk_count()` is outside `occupancy_of`'s bounds for a source ( below `SEGMENT_MIN_CHUNKS`, above `effective_cutoff`, or unreported ). That outcome is already determined at that point -- `occupancy_of` will return `None` and the whole thing will decline -- so this only stops paying for a conclusion already reached. `chunk_count()` is `O( 1 )` off the cached plan. **Do not generalize it to `Expr::Set` operands**: their behaviour was never measured here and the threshold was derived for sources.

**DONE 2026-09-13.** The short-circuit is in `compute_segments`, immediately after the `cheap_to_reopen` check and before the span collection, mirroring `occupancy_of`'s source arm exactly -- below `SEGMENT_MIN_CHUNKS`, above `effective_cutoff`, or unreported. **Measured effect: exactly -11 allocations per evaluation below the threshold ( 165 to 154 at 22 chunks per operand, 417 to 406 at 88 ), and byte-identical above it ( 834 and 1 464 unchanged )** -- so it recovers precisely the cost that adding `Expr::Source` to `cheap_to_reopen` introduced, and touches nothing that segments.

**The saving is small and its severity remains un-derived**, since the "~100 opens per evaluation" model came from the retracted claim and no caller has been measured to replace it. It is justified on the wasted-work argument alone: declining after opening every operand is wrong whatever the caller.

Guarded by `segmentation_still_engages_for_sources_above_the_threshold` in `tests/allocation.rs`, which is a **lower** bound rather than a cap -- unusual for that file, and necessary because the failure mode is an optimization silently not happening. Its first version asserted `>= 800` and caught nothing, being below both the segmented figure ( 1 038 ) and the suppressed one ( 910 ); the floor is now 970.

### Storage

- [ ] **rerun-the-unwired-sweep** ( *re-run 2026-09-14; absorbed `triage-the-remaining-sweep-hits` on 2026-09-12* ) **RE-RUN 2026-09-14, rebuilt from the construction recorded below, and it found one new hit within a day of the symbol landing.**

  ```text
                    2026-09-12   2026-09-14
    files                  240          241
    pub fn declared        685          693
    no caller anywhere      15           16
    called only by tests    82           85
  ```

  **The new dead symbol was `KeyStream::chunks_remaining`** ( `yesno-core/src/db/keystream.rs` ), which appeared **exactly once in the whole workspace** -- its own declaration -- one day after `Snapshot::key_stream` landed. **Closed 2026-09-14, and the resolution was not the one the dead-symbol framing suggests.**

  It was not unneeded API awaiting a semver decision. `ChunkStream::stats` **open-coded the identical subtraction** ( `(n - self.idx) as u64` ) three screens below it, so this was the sweep's *third* signature -- a `pub fn` whose production equivalent is computed inline elsewhere -- wearing the costume of the first. `stats` now calls it, which gives the accessor a real caller and collapses two expressions for one quantity into one. Covered by `chunks_remaining_counts_down_exactly_and_never_underflows`, which pins that the two agree at every step and fails under a deliberate drift.

  **A caller sweep cannot see this class, and that is the transferable part.** Both the duplicate and its original are live, correct and tested; nothing is slow and nothing is wrong. The only signal was a symbol appearing in the dead list whose body turned out to be an expression production was already writing by hand. So a dead-symbol hit is worth reading twice: once as "should this exist", and once as "is production doing this already".

  Both controls were run before the numbers were believed, as this entry requires. A `pub fn` planted in `yesno-server-utils/src/lease.rs` moved the dead count **16 -> 17** and the file was restored byte-identical; `token_digest` and `apply_into` appear in neither list, so a wired symbol is not flagged. **The turbofish guard reproduces exactly as recorded**: at `pub fn` scope removing the `::<` alternative changes nothing, while in wide mode it re-flags `apply_into` by name -- so the guard is real and must stay.

  **The wide-mode figures from this run are not comparable to the recorded 2 932 / 1 214** and are not offered as a replacement. This implementation's declaration pattern differs, giving 1 840 declarations, 49 no-caller and 99 test-only. Per this entry's own rule -- do not compare a run's count against an earlier one without stating which set it used -- the wide mode here is only the harness the turbofish control needs, exactly as recorded, and its absolute numbers should be re-derived rather than quoted.

  **A category worth naming, though its first example was withdrawn: API built on a downstream request the requester has not yet called.** Verified for `WriteBatch::merge_set` -- every call site is in a `#[cfg(test)]` block, no production caller. **But the framing was retracted by the consumer within the hour, and the retraction is the useful part**: they had read `merge_set` as a bulk-write primitive for columnar ingest, measured before rewriting, and found it **1.1x** against one-insert-per-ordinal ( 531 ms against 598 ms over 8.4 M inserts ). Its own doc comment says why -- it folds onto the ordinary insert path so the live apply and the replay apply are one operation. It is an **atomicity** primitive, not a bulk-write one, and it is doing exactly what it says.

  So `merge_set` is uncalled because nothing needs it yet, which is a different thing from uncalled-while-the-cost-it-addresses-persists. **`bitmap_words` remains a real instance** of the latter: filed, built, written into the consumer's plan, unused for two milestones while the cost it addressed stayed their bottleneck, then **4.2x the day it was consumed**. One instance, not two.

  What survives at full strength is the consumer's formulation -- **asking for an API is not using it** -- and a sharper corollary from the retraction: *asking for an API is not evidence of understanding it either*. The premise that justified the request was wrong, and it took a measurement rather than a reading to find that out.

  The `matrix/` group and the Flight-client escape hatches are unchanged and remain a semver judgment rather than a rule violation. `cert_digest` and `with_metrics` re-checked and both negative results still hold.

  **Prior run, 2026-09-12** ( *absorbed `triage-the-remaining-sweep-hits`* ) **The per-symbol verdict list it used to carry is deleted, on purpose.** That list existed so a later sweep could start from a verdict rather than re-derive one, and both halves of that premise failed: within two weeks **9 of its 15 verdicts had expired**, every one in the direction where a symbol had *acquired* callers, so it understated how wired the crate was; and the sweep now costs **0.25 s** rather than ~90 s, so caching its output is strictly worse than repeating it. The 6 verdicts that had not expired were re-checked on 2026-09-12 and all still hold. The four limits that sweep structurally has -- verdict staleness, path-based caller classification, common-name false negatives, and the vacuity of the obvious type-keyed variant -- are consolidated in LTM under measurement methodology, together with the rule that a dead function may be carrying a check production lacks.

**RE-RUN 2026-09-06 — and the recorded sweep was scoped wrong.** The ten-line script scans `yesno-core/src` for declarations *and callers*, which was right when core was standalone and is wrong now: its `pub fn`s are a **library API** whose callers live in the satellite crates. Scoped to core alone it flags 28, almost all false — `promote_database`, `snapshot_at`, `read_floor` and friends are called from `yesno-server` and `yesno-e2e`. Re-scoped to all `yesno-*` crates ( 236 files ): **9** called only from tests/benches, **4** with no caller at all. One more trap found in the sweep itself: it must match **turbofish** call sites — `simd::apply_into::<{ OP_AND }>( .. )` has no `(` after the name and was reported as dead when it is the hot path of every bitmap set operation. Do not act on a hit without grepping for it; this is a triage list, not a verdict.

**RE-RUN 2026-09-12, with controls** — 240 files, **685** distinct `pub fn` names, **15** with no caller anywhere and **82** called only from tests.

**The first run of the day reported 19 test-only, understating it more than fourfold.** It classified callers by **file path**, so an `assert_eq!( m.segment_count(), 1 )` inside a `#[cfg(test)] mod tests` block in `src/` counted as a *production* caller -- and any `pub fn` exercised only by the unit tests beside it was therefore classified as wired and omitted from **both** output lists. Found by cross-checking this entry's own "still holding" verdicts: four of them ( `BitmapContainer::toggle`, `Rebuilt::slot_count`, `VersionOracle::state_of`, `SegmentedMmap::segment_count` ) appeared in neither list, which looked like four expired verdicts and was instead the instrument lying. The verdicts hold; the sweep did not.

**The dead set is unchanged at 15**, which is the expected shape: a symbol with no caller anywhere does not care where its callers live. Only the test-only figure moved.

`SegmentedMmap::segment_count` is additionally **`#[cfg(test)]`-gated itself**, so it is not public API at all and correctly leaves the declared set -- which is why the declared count fell from 688 to 685 rather than staying put.

**The largest newly-visible group is `matrix/`** -- `argmax`, `argmax_by_row`, `echelon_gf2`, `solve_gf2`, `top_k`, `swap_row_pair` and some fifty more are exercised only by the unit tests beside them. Not a defect claim: a public matrix API is a library feature and a caller-less one is a semver judgment rather than a rule violation. It is recorded because the previous sweep could not see it at all. It also corroborates by construction -- the six items the `bignum` work listed as having test-only callers ( `int_is_zero`, `chunk_aligned`, `mul_u64`, `to_u64`, `add_mod`, `sub_mod` ) all appear now and **none** appeared before, which is why that list had to be derived by hand.

**The construction, because the instrument does not survive** ( it lives under `.agents-workspace/`, which is git-ignored, and this project's rule is that the finding is the deliverable ): glob `yesno-*/**/*.rs` excluding `target`; a file is test-bench if any path component is `tests`, `benches`, `examples` or `fuzz`. Declarations are `^\s*pub fn ([a-z_][a-z0-9_]*)` over production files only. A call site is `\b[a-z_][a-z0-9_]*\s*(\(|::<)`, counted once per line, with lines that declare a `fn` skipped in production files. **A production file must first be split at its `#[cfg(test)]` blocks** -- enter at such a line, count braces, leave when the depth returns to zero -- with those regions counted as **test** uses and any declaration inside them dropped from the API surface entirely. Omitting that step is the fourfold error above, and it fails *silently* in the reassuring direction: everything looks wired. Classify: zero production and zero test uses -> no caller; zero production and some test uses -> test-only. A single pass over the files rather than one per name takes the whole sweep from ~90 s to **0.25 s**, which is what makes re-running it as a control affordable.

**Controls, because this entry's own says not to trust a sweep that has not been checked against a known-unwired subject.** A `pub fn` planted in `yesno-server-utils/src/lease.rs` is flagged ( 15 -> 16 ) and the file was restored byte-identical. `token_digest` and `apply_into` appear in neither list, so a wired symbol is not flagged.

**And the turbofish justification recorded here was wrong about *this* sweep, which the control is what exposed.** Removing the `::<` alternative changes **nothing** at `pub fn` scope — the same 15 and 19 — because `simd::apply_into` is declared `pub(super) unsafe fn` and `^\s*pub fn` never captures it. Widening the declaration pattern to every `fn` *does* make the control redden, and it re-flags `apply_into` by name exactly as recorded, so the guard is real and must stay. But the wide mode is **not** a usable triage list — 2 932 names and 1 214 "no caller", dominated by trait-impl methods that are called through dispatch and can never match by name. Its only role is as the harness where the turbofish control can fail.

**So the two visibility sets answer different questions and their figures are not comparable.** `pub fn` alone asks "what public API has no caller", which is a semver question; including `pub(crate)` / `pub(super)` / private asks "what code is dead", which is a deletion question and needs a dispatch-aware tool this one is not. Do not compare a future run's count against 15/19 without stating which set it used.

**Two negative results are the valuable part of this run, because both were candidates for the `db_uuid` bug.**
  * `yesno-server`'s **`cert_digest`** has zero callers, and the module header says a client certificate is matched by fingerprint — exactly the shape of a stated purpose enforced by nothing. **It is not that.** `Authenticator::by_cert` computes `sha256_hex( der )` inline and compares it constant-time against `Entry::cert_sha256`, so enforcement is real; `cert_digest` is an unused operator-facing wrapper whose `token_digest` sibling *is* called ( from `yesno-e2e/src/server.rs` ). Neither is reachable from a CLI subcommand, so an operator computes the digest with `openssl` and `sha256sum` either way.
  * `yesno-server-utils`'s **`with_metrics`**, documented "for the E2E harness", is called by nothing including the harness — which reads archive counters **in-process** rather than scraping them. But `ArchiveMetrics::render` is directly unit-tested, so the uncovered surface is only the sidecar's thin HTTP `serve` path, not the metric values.

The remaining 13 no-caller hits are mostly documented escape hatches on the Flight client ( `inner`, `inner_mut`, `into_parts`, `flight_info`, `into_flight_info` — "for example to add auth metadata" ) and alternate constructors. Still a triage list and still not a verdict: deleting public API is a semver decision, and this sweep is not the place to take it.

**All three earlier hits were removed on 2026-09-06 and that list is stale** ( verified 2026-09-09: no `fn reopened`, no `PrefixSketch::n`, no `WalPublisher::base_lsn` anywhere in the workspace, and `slabmeta.rs`'s header already records the deletion and explains why the region existing is the real fix ). `WalPublisher::new`'s `base_lsn` is **no longer always `0`** either -- `yesno-server/src/replication/mod.rs` passes `next` -- so the fourth-signature instance is genuinely resolved rather than merely invisible. Kept below for the reasoning, which is still the model for a removal.

  **The three hits as recorded** ( public API removal is a semver decision and should be its own diff, per the `sink-layout-accessors-have-no-callers` precedent closed earlier the same day ):

  * `Allocator::reopened` — **read the history before deleting.** It was the fix for a data-loss bug ( JOURNAL 2026-08-25, bug 5 ): the allocator came back from a reopen with *no slabs*, so the first allocation claimed slab 0 and wrote over live extents. It is uncalled today because the open path builds a real restored slab list from `SLAB_META`, which is strictly better than its opaque-slab stand-in — so it is a **superseded intermediate fix, not a regression**. Verified: the reopen path restores per-slab occupancy, and `tests/allocation.rs` reopens before reading. The `slabmeta.rs` header still cites it by name and must be updated in the same diff.
  * `PrefixSketch::n` — accessor, zero callers.
  * `WalPublisher::base_lsn` — accessor, zero callers, and **the same field the fourth signature below already flags as always `0`**. Two independent signatures landing on one construct is the strongest evidence in this item; take them together.

**Blindness check performed 2026-09-07**, as this file's sibling entry `triage-the-remaining-sweep-hits` demands ( "do not re-run a sweep of either kind without first checking it against a known-unwired subject -- both versions of this one reported a clean tree while blind" ). A canary `pub fn` was planted with no caller anywhere:

  * the **workspace-scoped** zero-caller pass **flagged it** — so the list that produced the three real hits above is not blind;
  * the **core-only** pass **did not** , and the reason is a predicate bug rather than scope: it selects `test_callers > 0 && prod_callers == 0`, which finds "called only from tests" and is structurally incapable of finding "called by nothing at all". Two different questions that look like one. Keep both passes, and know which one you are running.

  Original entry: ten lines of Python ( JOURNAL, 2026-08-25 ): flag every `pub fn` in `yesno-core/src` whose only callers live under `#[cfg(test)]` or in `tests/`. **Ten findings across four runs**, all resolved: `Allocator::reclaim`, `evacuation_candidates`, `Memtable::prune`, `CheckpointPolicy`, the whole `wal` module, `fsck`, the duplicate `mvcc::SnapshotRegistry` ( deleted ), and the `ExtTrailer` / packed-header identity checks ( written and wired ). The fourth run ( 2026-08-25 ) added two more in a **new costume**: `ops::card`'s unspecialized mixed arms ( a legitimate *duplicate* implementation, which the unwired-caller signature cannot flag ) and `wal::record::{encode,decode}_chunk_delta` — a WAL record type with a discriminant, an encoder, a decoder, unit tests and **no production caller**, while the commit path logged one 72-byte `SetRange` per ordinal. `checkpoint::packed_live_totals` was deleted as a third implementation of accounting two other places already do. Re-run after any milestone. **Three** signatures: a `pub fn` whose callers are all tests; a test whose only subject is a function production no longer calls; and — new — a `pub fn` that production calls *a slower equivalent of*. The third needs a cost benchmark, not a caller sweep. Two: a `pub fn` whose callers are all tests, **and** a test whose only subject is a function production no longer calls.

**A fourth signature, found 2026-08-28, that no caller sweep can see: a parameter whose every call site passes the identity value.** The function is called, the parameter is read, the constant has a documented default — and the feature does not exist. Four instances, all in the WAL:

  * `WalPublisher::new( shard, bytes, base_lsn )` — always `0`.
  * `SuperBlock::wal_replay_lsn` — always written `0` by `Db::checkpoint`.
  * `ShardLog { base_lsn }` — always `0` in `replay_logs`.
  * `CheckpointPolicy::wal_bytes` — `should_checkpoint( dirty, 0, elapsed )`, so a documented 1 GiB trigger was unreachable.

  The first three were one unbuilt feature ( global LSNs ) whose plumbing was complete; the fourth cost a workload rewriting a small working set **448 KB of log against a 64 KB threshold and zero checkpoints**, because the dirty trigger measures the memtable and cannot see it.

Grepping the constant finds nothing wrong — it is defined, it has a sensible value, it is referenced. The tell is on the **call side**: an argument that is a literal at every call site. That is mechanically checkable and nothing checks it. Worth a second ten-line script next to `sweep.py`; the calibration rule is the same — inject one genuinely-always-identity argument and one that merely defaults, and confirm both verdicts before believing the output.

**The instrument has a quantified blind spot, and quantifying it was the point.** Keyed by bare name it skipped every function defined more than once -- including all **74** `fn new`, and constructors are exactly where these parameters collect. Qualifying by the enclosing `impl Type` fixed that ( definitions 1 625 -> 2 085 ) but made method calls unattributable without type inference, which regex cannot do. Current honest reading: **0 hits, 2 085 definitions, 16 023 call sites attributed, 9 286 unattributable, 39 names disqualified for ambiguity**. A name with any unattributable call site is *excluded*, not cleared -- otherwise "0 hits" would hide the gap.

**The two keyings have complementary blind spots and both should be kept**, exactly as the core-only and workspace-scoped caller sweeps above do: bare-name attributes more call sites but cannot see overloaded names; impl-qualified sees constructors but drops what it cannot attribute. Do not read either one's silence as a clean tree on its own.

**One diagnostic was itself wrong before it was right.** The first "unattributable" count read **40 291**, because it counted every `identifier( .. )` the regex matched -- `Some( .. )`, macros, closures -- rather than only names that are actually our functions. It claimed a blind spot forty thousand sites wide that did not exist. A miscalibrated diagnostic is worse than none: it would have justified abandoning the approach.

**The second script was written and run 2026-09-09.** 0 hits across 1 625 definitions and 25 309 call sites, after one real finding was closed. Construction and calibration are recorded in `LTM/measurement-and-investigation-methodology.md`; the instrument itself lives under `.agents-workspace/tmp/identity-args/` and self-checks with `--calibrate`.

**The prescribed calibration — one always-identity argument and one that merely defaults — was not enough, and the gap produced a false positive on real code.** The call-site regex could not see **path-qualified** calls ( `record::encode_set_range( .. )` ), so `encode_set_range( .., remove )` was reported constant `false` across "all 3" sites when it has **8**, two passing `true`. The canary called everything bare. Fixing it raised coverage 19 515 -> 25 309 call sites: **30 % of all call sites had been invisible**. The canary must contain every call *shape* the corpus does — bare, method ( positions shift past `self` ), and path-qualified — not merely a positive and a negative case.

**A separate blindness: the very first run printed `0 parameter(s)` having scanned `0 definitions`**, because the path filter wanted a leading slash that relative paths do not have. Printing the definition and call-site counts beside the verdict is what caught it. Always print what the instrument looked at, not only what it found.

**The one real hit**: `RetentionFloor::holders( shard )` was `0` at all six call sites, every one a test. Replacing the filter with `true` — counting every shard's followers — broke **no test in the workspace**. Closed by `holders_counts_one_shard_and_not_the_others`, which fails under that sabotage, and the method's "For metrics and for tests" doc was corrected because nothing renders it. *A parameter that only ever takes one value is a branch that is never taken* — a test-coverage signature as much as a dead-feature one. -- *source: 2026-08-25, extended 2026-08-28*

- [x] **internal-key-compression** ( *restored 2026-09-14, **measured and closed the same day** -- and the measurement contradicted the figure the entry was built on* ): internal B+tree levels carry **full 14-byte separators** while leaves carry prefix-compressed suffixes.

  This entry said the asymmetry buys "**~0.25%**" and asked for a measurement contradicting it before being opened. **It is 1.83%, 7.3x higher**, and the error is identifiable: the formula `1/fanout` is right and the fanout is the wrong one. The ratio depends on the **internal** fanout, not the leaf fanout. `INTERNAL_ENTRY` = 18 B gives 55 entries per 1 KiB node, and `1/55` = 1.82%.

  ```text
    ksuf   leaf cap    leaves   internal   internal %
       2         99     10102        189       1.837%
       4         82     12196        228       1.835%
       8         62     16130        301       1.832%
      12         49     20409        380       1.828%
      14         45     22223        414       1.829%
  ```

  **Flat across every legal suffix width**, because the leaf fanout cancels out of the ratio -- which is exactly why a leaf-side intuition produces a wrong answer, and why the entry's own suggested falsifier ( "a corpus whose fanout is far smaller than assumed" ) could never have worked: no corpus moves this number.

  **Closed anyway, and that is the point.** Halving the separator roughly doubles internal fanout and saves ~1% of *index* bytes -- 4x the recorded payoff -- but index nodes are a few percent of the file, so it is **~0.05% of the database** for a format change and a meaningful jump in subtlety. The verdict is unchanged and now rests on something true. Construction: 1e6 index entries, `INDEX_NODE` = 1024, `HEADER` = 32, `leaf_entry_size` = ksuf + 8, internal capacity `( n - HEADER - 4 ) / 18 + 1`. -- *source: recovered from `yesno-core/src/index/tree.rs`, measured 2026-09-14*

- [ ] **planner-overlap-estimation** ( *restored 2026-09-14; cited from `stream/plan.rs` and recorded nowhere* ): `disjoint( a, b )` is decided from `bounds`, so it is **one-directional**: `true` means provably disjoint, `false` means unknown. That asymmetry is the safe one -- a missed disjointness costs an optimization, a false one changes results.

  It is also the weak point of the whole cost model, and this is the entry that says so. Two sets can share an interval completely and still be disjoint chunk for chunk, and min/max bounds cannot tell those apart, so overlap is only ever **detected**, never **estimated**. Estimating it needs chunk-level prefix statistics rather than an interval -- which puts it directly against `statistics-earn-nothing` and the repeated measurement that planner statistics do not pay. Read it together with `aligned-chunk-views`, whose conclusion is that `ChunkProfile` should drive the *walk* rather than gate a rewrite; an overlap estimate is the same statistic being asked for by the same planner that has three times failed to profit from one. -- *source: recovered from `yesno-core/src/stream/plan.rs`, 2026-09-14*

- [ ] **planner-cost-is-o-chunks** ( *restored 2026-09-14; cited from four sites, three of which tell a reader not to trust the module until it closes* ): **planning does not stay small relative to execution.** There is no cache anywhere in `plan.rs` and every fixpoint pass rebuilds the statistics at every node, so `plan` is `O( chunks )` in the operand size.

  **The statistics half is closed** by `STATS_MAX_CHUNKS` = 4096, which bounds statistics cost **per operand**. What remains is the planner's own tree walk: `pass()` recurses bottom-up and each node's guards call `is_empty_expr` and `disjoint`, both `O( subtree )`, so a wide tree still pays `nodes x passes` consults. That is per *query*, which is the dimension `STATS_MAX_CHUNKS` does not touch.

  **RE-MEASURED 2026-09-14 against the cap, and the unbounded half is closed.** Node count held at two leaves so this isolates the chunk axis; the `O( nodes^2 )` tree walk is `plan_scaling` in `benches/setops.rs`. `a AND b`, operands overlapping 3 of 4 ordinals per chunk, release, best of 5 batches:

  ```text
    chunks/op     plan ns     exec ns   plan/exec
         2048       45 923      71 005       0.647
         3072       72 724     107 348       0.677
         4095       99 207     143 218       0.693
         4096       99 325     142 186       0.699
         4097           95     143 084       0.001
         5120           94     181 167       0.001
         8192           94     301 667       0.000
       524288           94  30 390 596       0.000
  ```

  **Plan cost is linear below the cap ( ~24 ns/chunk ) and flat at ~95 ns above it**, and the transition is **one chunk wide**: 99 325 ns at 4096, 95 ns at 4097, a **1045x** step. Above the cap it is constant out to 524 288 chunks, the largest measured.

  **What the cap changed, stated precisely: not the shape, but where the shape stops.** The recorded 42 ms / 64 ms is a ratio of **0.66**; the worst ratio now measurable is **0.699**, at the cap. Essentially unchanged -- what moved is the *absolute* worst case, from 42 ms at a million chunks to **99 us** at 4096, because the peak can no longer occur past the cap. So "planning does not stay small relative to execution" survives as written, in a bounded window, and **plan never exceeds exec** ( max 0.699 ).

  **The "bigger query plans faster than a smaller one" property survives and is sharper than recorded.** It is not a gradual falloff: one additional chunk on either operand changes planning by three orders of magnitude. The recorded 300 000x was measured across a much wider span; the step itself is 1045x. A workload sitting near 4096 chunks per operand has a planning cost that swings 1045x on a boundary nothing in the query expresses, which is the residual worth naming.

  What the cliff implies survives the numbers being stale: the cheap path is cheap because it *estimates*, so closing this is not "make the exact path faster" but deciding what planning may spend as a function of what it can win. That puts it against `allowance-starves-the-rewrites-worth-most`, and `memoize-loads-not-statistics` answers it from below -- memoize the load and the per-pass rebuild stops being a rebuild. Read the three together.

  **An earlier version of `sketch.rs`'s header claimed `plan` "memoizes per `plan()` call". It does not, and never did.** That sentence is why the cost went unmeasured. -- *source: recovered from `yesno-core/src/stream/sketch.rs`, `stream/plan.rs` and `benches/setops.rs`, 2026-09-14*

- [ ] **cost-model-cannot-see-materialization** ( *found by arbitrating a neighbouring session's report, 2026-08-27* ): `cardinality_cost` rates `And( ¬p, ¬q )` and `¬( p ∪ q )` **equal**, and they differ **214x** in execution. Measured in release, 2 000 iterations, both answering 65 535, with `p`/`q` 2 000 chunks each and a one-chunk complement window: `And( ¬p, ¬q )` **53 095 ns**, `¬( p ∪ q )` **248 ns**. The planner declines the rewrite and takes the slow form.

  The mechanism is that two operators answer `cardinality` by routes the cost model does not distinguish. `Not::cardinality_dyn` seeks to its window, stops at `last_prefix()`, and counts by **subtraction** — `( hi - lo ) - |input ∩ range|` — building no container. `And::cardinality_dyn` has to materialize both operands and intersect, and here that is two 65 535-element complements. `cardinality_cost( And )` is `min( yield_chunks )` over its operands, which charges nothing for materializing an operand that cannot be counted arithmetically.

**Not fixable by loosening `cheaper`.** Both forms price at 1, so the rewrite is declined for want of a *strict* decrease — and strictness is what gives `plan` its termination argument ( every accepted cost-guided rewrite strictly decreases a non-negative integer ). Also not fixable by the `Not` clamp that exposed it: that made the model *more* faithful and moved no decision. What is needed is a cost model that charges an operator for operands it must materialize, which is a real modelling change and interacts with `disjoint-or-is-overcharged` and `allowance-starves-the-rewrites-worth-most`. Land it with `tests/allocation.rs` watched — the last cost-gate change made on a two-operand measurement sent an 8-way `Or` to 27 538 allocations. Characterized by `de_morgan_is_declined_here_and_that_is_currently_wrong` in `plan.rs`, which pins **current** behaviour and says so. -- *source: 2026-08-27*

**Blocker re-verified 2026-09-12 and it still holds.** `cardinality_cost` remains `Expr::And( a, b ) => yield_chunks( a ).min( yield_chunks( b ) )`, charging nothing for an operand it must materialize, and the characterizing test is still there. Unlike `fallible-posting-source`, whose stated blocker had quietly been answered elsewhere, this one is a genuine modelling change: it needs a new dimension in the cost function and it interacts with two other open entries. Do not attempt it as a local edit to `cardinality_cost`.

**Corpus measured 2026-09-12, and it changes the shape of the fix.** The entry rests on **one** pair. A family of ten shapes was measured — `cardinality_cost` against `cardinality_dyn` nanoseconds, planning hoisted out of the timing loop — and the single pair understates the problem:

  ```text
  shape                          cost           ns      ns/cost
  And(¬p,¬q) 1-chunk                1        47384      47384.4
  ¬(p∪q) 1-chunk                    1          421        421.0
  And(¬p,¬q) 32-chunk              32       425413      13294.2
  ¬(p∪q) 32-chunk                  32          857         26.8
  And(p,q) plain                 2000        75025         37.5
  Or(p,q) plain                  8000        24053          3.0
  And(dense,dense)                  1          342        341.5
  And(¬dense, p)                    1          613        612.6
  AndNot(p,q)                    2000        73819         36.9
  Xor(p,q)                       8000        71713          9.0
  ```

**Two independent inversion families, not one.**
  * `And` over complements — the recorded case, up to **18x** at 32 chunks.
  * `And( p, q )` priced **4x cheaper** than `Or( p, q )` and running **3x slower**, with no complement anywhere. This one is not in the entry above and was not visible from the De Morgan pair.

**One root cause, and it is the one the entry names**: `And::cardinality_dyn` must materialize its operands; `Or` / `Xor` count by merge and `Not` by subtraction. So `And` is **systematically** undercharged, not merely undercharged over complements.

**A single scalar constant cannot fix it.** `ns/cost` spans **3.0 to 47 384** — a factor of ~16 000 — across shapes the model is meant to order. Fitting a constant to the De Morgan pair leaves the `And`-vs-`Or` family wrong and can easily make it worse, which is exactly how the 27 538-allocation regression happened.

**What the fix needs, now evidence-backed**: a second dimension charging an operator for operands it must *build*, keyed on the materialized cardinality rather than the chunk count. `Not::cardinality_dyn` already computes `( hi - lo ) - |input ∩ range|`, so the quantity exists. Re-measure this corpus **and** `tests/allocation.rs` **and** the `union_gate` bench before and after; the corpus is cheap to rebuild from the construction above.

**One cross-reference in this entry is stale**: `disjoint-or-is-overcharged` was closed 2026-08-27, so the `Or` arm already runs the strict-span test and the `Or` numbers above are the honest overlapping-merge case.

### End-to-end

- [ ] **validate-september-13-core-api-changes-with-gate-pg**: run `./scripts/gate-pg.sh` on a Docker host against the current tree. The `Snapshot::key_stream` / `key_expr`, `WriteBatch::merge_set`, `unstable_arrow::bitmap_words`, and `Counts::row` changes passed the Cargo gate but were never validated through the independent Bazel build that also compiles `yesno-core` and `yesno-flight`. The routine gate does not subsume this check. -- *source: JOURNAL `2026-09-13 -- a consumer's bit-sliced-lens proposal audited, and Snapshot::key_stream built from its §7`*

- [ ] **no-session-guarantees-on-the-flight-surface** ( *2026-09-12, from the OLTP assessment; **both halves of the original entry landed 2026-09-12** — what is left is narrower and named below* ): **`do_put` now reports the commit version.** `app_metadata` widened from 8 bytes to 16 ( rows, then version ); `Ack { rows, version: Option<u64> }` decodes 8 *or* 16 so a new client against an old server loses the version rather than the call, and `Some( 0 )` never occurs because no commit is assigned version 0. New client methods `insert_acked` / `remove_acked` / `insert_batch_acked` / `remove_batch_acked`; the four original methods keep their `u64` return. Only the `*_batch_acked` pair has an unambiguous version — one batch is one commit — while the streaming forms commit per batch and report the **last**, a state containing every row but not one atomic instant for them.
**And `GetFlightInfo` no longer refuses a version that is merely not visible yet.** It waits `visibility_wait` ( default **1 s**, `with_visibility_wait` to change, `ZERO` to restore the old behaviour ) in a `spawn_blocking`, then calls `snapshot_at` as before. Still **exact**, not "at least this new": a coordinator fanning one query across endpoints depends on the version being honoured, and the wait only removes a refusal that was purely about timing. Pinned by the paired `a_read_at_a_not_yet_visible_version_waits_instead_of_refusing` / `without_the_wait_the_same_read_is_refused_as_not_visible`, which issue the identical request and get `Ok( v2 )` and `version 2 is not visible` respectively.
**Default 1 s is a stated policy, not a tuning knob.** The window it closes is one fsync; a larger gap is replication lag or a version this database never assigned, and neither should be hidden behind a server-side block holding a `spawn_blocking` thread.
**The commit version is exposed on four of five clients, and the fifth is a reasoned gap rather than an oversight.** Rust, Python, Go and Java each gained an acked ingest form because each already has a version-bound query to pass the version to ( `prepare_query_at` / `PrepareQueryAt` / `prepareQueryAt` ). The C++ client's read surface is `PrepareKey` / `Fetch` / `Get` / `Cardinality` with **no expression form and no at-a-version form**, so a commit version there would have no caller; it decodes the field and stops. Expose it when that client gains a version-bound query, and not before.

**The `yesno` CLI closes the loop from a shell.** `put` prints the version it committed at, and `query` gained `--at-version N`, so an operator can read back their own write without writing a client. Verified end to end against a live daemon: `put` reported v1 then v2, `query 1 --count-only --at-version 2` answered 4, and a fabricated version errored after the bounded wait. `get` deliberately did **not** gain the flag — it takes a key, not an expression, and the versioned descriptor form carries an expression; adding it would mean a second encoding for no new capability.
The trap there is silent: `--at-version` must encode a **`QueryRequest`**, not a bare `SetExpr` with a version attached, because a bare expression has no version slot and the server tells the two apart by magic. The wrong encoding asks "read at the newest visible version" and **answers it successfully**. `a_versioned_query_descriptor_round_trips_its_version` pins both directions — the versioned form must decode as a request carrying the version, and the unversioned form must *not* be a request, without which the test would pass for a helper that ignored its argument.
**Seven consumers decode this one field, across five languages, and the blast radius was under-reported three times before it was enumerated properly.** See the `Gate coverage, 2026-09-12` section above for the list, the three distinct traps behind it, and why grepping for the field name finds three of seven.

**What remains**: a genuine *minimum*-version request form, distinct from the exact one. The two differ exactly when a checkpoint has reclaimed the named version — exact must then fail `VersionReclaimed`, while "at least V" could legitimately answer from a newer snapshot, since every newer state contains the write. Serving both needs a new `Request2` variant and its wire encoding, and a decision about which one a bare `QueryRequest.version` means; do not overload the existing field to mean both, because the fan-out use depends on exact and would silently degrade.
- [ ] **no-idempotency-key-on-the-write-path** ( *2026-09-12, demonstrated* ): mutations carry no request id and nothing dedups, so a retry after an ambiguous failure is indistinguishable from a fresh intent. Demonstrated by `a_retried_insert_resurrects_an_ordinal_another_client_removed`: a retried insert restores an ordinal another client deliberately removed, and — the sharper half — reports `true` ( "newly added" ), the same answer a genuinely needed retry gives, while an *uncontested* retry reports `false`. The one signal available to the client points the wrong way at exactly the moment the retry did damage. Also applies symmetrically to a retried delete removing a later re-insert. A per-request id carried on the batch and remembered for a bounded window would make a duplicate detectable; that is a real design item, not a signature change, and it interacts with `no-session-guarantees-on-the-flight-surface`.

- [ ] **no-conditional-write** ( *2026-09-12, from the OLTP assessment* ): there is no compare-and-set, no expected-version argument, and `WriteBatch::store_set` is a blind whole-key replace, so a client read-modify-write can lose a concurrent update with no error on either side. Engine-level operations do **not** lose updates — insert/remove are commutative set ops under the shard lock, pinned by `concurrent_writers_to_one_key_do_not_lose_updates` — so this is purely an application-level hazard, and one the set-oriented API mostly avoids by construction. It matters for `store_set` and for any client computing a new set from a read.

- [ ] **miri-cannot-reach-the-mmap-unsafe-sites** ( *restored 2026-09-14; `scripts/miri.sh` asserts this item exists and it did not* ): `store::segment` appears in neither MIRI tier, and not for slowness -- MIRI physically cannot execute it: *"Miri does not support file-backed memory mappings"*. **The uncovered surface is 25 `unsafe` blocks and 12 `unsafe fn` across six files**, not the two this entry claimed on the day it was restored: 4 mmap blocks ( 2 in `store/segment.rs`, 2 in `db/readers.rs` ), 2 libc FFI calls ( `kill`, `__errno_location` ), and 19 SIMD-intrinsic blocks in `ops/{array,bitmap,mixed,run}.rs`. MIRI reaches none of them -- file-backed mappings, arbitrary FFI and target-specific intrinsics are all outside it.

  **The "two blocks" figure was copied here from `scripts/miri.sh` and was already three weeks stale**, having been written before the NEON arms landed on 2026-08-27 and 2026-09-07. Both copies corrected 2026-09-14. The conclusion is unchanged and slightly stronger -- the sites with the most need of a UB checker are the ones MIRI cannot reach -- but the gap is twelve times the size the rationale stated.

  **The substitution named here was tested on 2026-09-14 and it does not hold.** This entry said the gap "is covered instead by AddressSanitizer and Valgrind in the `deep` gate". Measured, with a heap control in the same binary so a null result could not be "the sanitizer was not enabled":

  ```text
    probe                                          ASan      Valgrind
    heap overread, 64 B past a 4 B Vec             CAUGHT    CAUGHT
    mmap overread, 1000 B past a 16 B slice,
      still inside a 4096 B file mapping           silent    silent
  ```

  Both tools catch the control and **both return the mmap overread silently, exit 0**. The reason is structural rather than a configuration mistake: ASan instruments allocations *it* makes, and a file mapping is not one; Valgrind treats the whole mapping as valid addressable memory. So the four mmap blocks are covered by **neither MIRI nor the tools standing in for it** -- not weaker coverage, none.

  **Out-of-mapping access is nonetheless guarded twice, which an earlier draft of this entry got wrong.** `segment_for` refuses it via `covers()` as a typed error, and the pointer is then derived from `seg.as_slice()[off..off + len]` -- a **safe, bounds-checked slice index** that panics if `covers()` were ever wrong. Both are pinned by `out_of_range_access_errors_rather_than_panicking`. So the `unsafe` is narrower than the module header suggests.

  **The hazard that survives both guards was wrong-but-in-range**, and it is the one the sanitizers also cannot see. A `ChunkRef` naming a valid cell with a plausible length belonging to a *different* extent passes `covers()`, passes the slice index, and aliases a neighbour's bytes. `ChunkRef::validate` runs on the read path but checks **format bits** ( reserved kind 3, unimplemented `enc` ), not whether an offset is the right one; and a `Run`'s length is read from its own payload prefix, so a corrupt payload chooses its read length.

  **Closed later the same day by `stored-page-crcs-are-not-verified`'s read-path cache**, which this finding is what argued for. The stored CRC catches exactly this case and is now recomputed where the bytes are read, once per faulted region, for all three families. So the residual is no longer "nothing sees it" but "a CRC sees it, and no UB checker does" -- which is the honest standing position, and the reason `SegmentedMmap`'s containment check and the I2/I6 invariants stay load-bearing rather than becoming belt-and-braces.

  **What remains genuinely uncovered is narrower**: a corruption that preserves the stored CRC, which means a corrupted payload *and* a correspondingly corrupted trailer. That is outside what a checksum can promise and is not a gap this entry can close.

  **The blindness follows the bytes, not the module.** Any slice that aliases the mapping inherits it, which by design includes a store-backed container's payload -- so the SIMD kernels in `ops/` are equally uninstrumented when their operands are disk-resident rather than heap-resident. The tools cover the heap-resident path only. This does not make the sanitizers worthless ( they cover 21 blocks' heap paths and found real defects ), but "ASan covers our unsafe" is not a sentence this repository can say.

  Do not resolve this by moving `store::segment` into a tier and finding it errors. `miri.sh` says it explicitly: reporting the gate as met by running MIRI over code that never touches a mapping is precisely what this item warns against. -- *source: recovered from `scripts/miri.sh`, 2026-09-14*

- [x] **dangling-backlog-citations** ( *2026-09-14, quantified and closed the same day* ): source and scripts cite backlog slugs that are recorded **nowhere** under `.agents/docs/`. A sweep of `yesno-*/{src,tests,benches}`, `scripts/` and `e2e/` found **25** such slugs, of which 9 are not slugs at all ( crate names, target triples, tokio flavours ) and **16 are genuine**.

  **The cause is mechanical, which is why it will recur without a gate.** `reconcile-journal-ltm` may remove JOURNAL entries once they are consolidated into `LTM/`; a slug dies with its entry while every citation of it in `src/` survives untouched, and nothing links the two. So the citations decay in the reassuring direction -- the comment still reads as though the reasoning is written down.

  **Closed 2026-09-14: the baseline went 16 -> 0 the same day it was created.** Five were repaired when the check landed and the remaining thirteen were triaged one at a time. Each remaining one needs a decision rather than a rewrite: **record the entry** if the work is open, or **repoint the citation** at where the reasoning actually lives if it closed. Do not close one by deleting the slug from the comment without reading what it was claiming -- two of the five closed here ( `multiprocess-read-only-reader`, `planner-overlap-estimation` ) named real, unrecorded design gaps, and deleting the citation would have destroyed the only remaining record of them. -- *source: TODO sweep, 2026-09-14*

- [x] **checkpoint-holds-the-store-lock-across-three-fsyncs** ( *2026-09-14; **DONE the same day*** ): `Db::checkpoint` formerly held `Mutex<ShardStore>` across three `fsync`s. `prepare_superblock` now gathers the allocator-dependent work under the lock, `run` performs the syncs without it, and `adopt_superblock` reacquires it with a `Durable` token. The three syncs still preserve the same data, metadata, then publication ordering, but readers can follow the old live root while they run.

  **PARTIALLY CORRECTED 2026-09-14, and the correction is the interesting part.** The attribution below was taken with **dirty state held at four keys** while resident size varied -- holding constant the quantity that actually drives the slope. Re-swept against dirty *ordinal volume* with resident size fixed:

  ```text
    dirty ordinals   checkpoint ms ( 3 runs )      verdict
              2 000   21.2  18.3  17.3            floor
            200 000   19.3  16.3  15.7            floor, indistinguishable
          4 000 000   37.6  34.8  34.8            ~2x the floor
  ```

  **The first version of this table was a single run per point** -- 16.7 / 17.9 / 20.2 / 25.2 / 40.8 -- and read as a smooth monotone rise. With three runs the bottom two points **overlap completely** and there is no measurable slope below a few hundred thousand dirty ordinals; the rise is real only at the top, and even there 20x the dirty volume buys ~2x the time. Strongly sublinear, not proportional.

  So there are **two components**: a **~17 ms floor** that is the three `fsync`s ( flat across three orders of magnitude of dirty state ) and a **shallow super-floor term** from the carry-forward walk and the tree rebuild, which only separates from the floor at millions of dirty ordinals. The floor is what the original measurement saw, because it was the only thing left when dirty was pinned at four keys. The consumer reports 85-165 ms stalls at ~65 000 documents per checkpoint over a 256-dimension code -- ~17 million dirty ordinals, an order of magnitude past the top row here -- where the super-floor term dominates and the floor is a minor part.

  **Consequence for the fix that landed**: releasing the store lock across the three `fsync`s removes the **floor** from the exclusive region and leaves the **slope** inside it. Measured benefit is therefore real but bounded -- worst-case reader stall 5.9-8.3 ms -> 0.5-0.7 ms on a light writer, and a shrinking fraction of the stall as write volume grows. Getting the slope out of the lock is a larger change ( the walk and `checkpoint::run` mutate the allocator ), though the same durability argument may cover it.

  Original attribution, taken with dirty pinned at four keys:

  ```text
    resident keys   dirty   checkpoint ms
              200       4            16.8
              800       4            15.0
             3200       4            11.9
            12800       4            16.6
  ```

  So the `O( dataset )` carry-forward walk in the same region is **not** the problem at these sizes; a fixed ~15 ms of fsync is. ( The walk presumably dominates eventually; it does not by 12 800 chunks. )

  **Independently cross-checked from the other side.** The ~15 ms was timed here by clocking `Db::checkpoint`; the consumer's one-reader row shows a worst query of **22.50 ms against a 1.82 ms median** -- about 20 ms of excess, with p99.9 at 18.28 ms indicating several queries landed there, one per checkpoint. Two surfaces, neither derived from the other: one timed the checkpoint, one timed a query waiting behind it. That is worth more than either alone, and notably more than the two agreeing signals that opened this investigation ( their 2.57x and my 0.22x ), which agreed while sharing a cause neither of them named.

  **The consumer's tail-latency numbers are the cost**, 8 concurrent readers with and without a writer, 32 shards: p99 5.95 -> 36.21 ms, **p99.9 6.85 -> 54.92 ms**, median essentially unchanged ( 4.81 -> 4.72 ). Median-blindness is why nothing had noticed. Their frequency is forced by an explicit `flush()`, so read the **per-event stall** rather than the rate; at policy frequency the same magnitude arrives roughly once a minute, because `WriteBatch::commit` calls `enforce_policy` which runs `checkpoint()` **synchronously on the committing thread** whenever the 60 s interval fires.

  **Why this looks tractable: the design's own ordering argument says readers need not be excluded.** During the three syncs `self.sb` is still the old superblock, the new superblock is written to the **inactive** slot, and new extents are unreachable from the old root -- so a reader following the old root sees a consistent old state throughout. The transition is already atomic at the flip. What enforces exclusivity is `&mut self` plus the `Mutex`, not a requirement of the durability protocol.

  This fix was sequenced before the `RwLock` question because it was independently justified at the existing `Mutex`. The remaining super-floor work is tracked separately below. -- *source: consumer tail-latency measurement plus local attribution, 2026-09-14*

- [ ] **checkpoint-super-floor-work-still-holds-store-lock**: releasing the checkpoint lock for the three syncs removes the roughly 17 ms fixed floor from the exclusive region but leaves the carry-forward walk, tree rebuild, slab-metadata writes, and allocator mutation inside `prepare_superblock` under `Mutex<ShardStore>`. Three-run measurements overlap at 2,000 and 200,000 dirty ordinals and separate only at 4,000,000, where checkpoint time is about twice the floor; a consumer writing about 17 million dirty ordinals per checkpoint still sees 85-165 ms stalls dominated by this remainder. Moving it outside the lock is a larger design change because it mutates allocator state and must preserve old-root reader consistency, checkpoint exclusion, and the `Durable` adoption ordering. -- *source: JOURNAL `2026-09-14 -- the checkpoint no longer holds the store lock across its fsyncs`*

- [ ] **read-concurrency-is-bounded-by-shard-count** ( *2026-09-14, measured* ): `KeyStream::next_chunk` takes `Mutex<ShardStore>` **per chunk** and holds it across the whole of `read_container_for` -- trailer read, checksum verification, `buffer_at` and the decode. Concurrent readers therefore serialize per shard, and read scaling tracks **shard count rather than core count**.

  ```text
    shards    1 thread    20 threads    scaling
         1     952.5        212.1        0.22x
         4     899.2        672.2        0.75x
        20     496.5       1743.6        3.51x
  ```

  Measured on 20 cores with one shared `Snapshot`, 3 776 reads per sweep. **At one shard it degrades rather than plateaus** -- 20 threads deliver a quarter of one thread's aggregate throughput. Note the knob has a cost: single-threaded is worse at 20 shards ( 496 against 952 ), so this is a tradeoff and not a free win.

  **Do not start by narrowing the critical section. That was this entry's first recommendation and the arithmetic contradicts it.** The section is **278 ns** ( 3.60 M reads/s single-threaded ). If one mutex merely *serialized* readers, 20 threads would still reach that 3.60 M. They reach **0.80 M** -- a **4.5x deficit beyond serialization**. So hold time is not the bottleneck; the overhead of contending for a short, hot lock is, and **shortening a section already too short only raises the acquisition rate**.

  The shard sweep corroborates it from the other side: at 20 shards the section is *longer* ( 533 ns ) and throughput is nonetheless **6.58 M reads/s**, because contention is spread over twenty mutexes instead of one. What pays is fewer contenders per lock, not a smaller lock.

  **The candidate is `RwLock<ShardStore>`, and it is smaller than it sounds.** All 13 shard-store lock sites were classified: **10 need only a shared borrow** -- every read path binds `let store = ...` and passes `&*store` ( `mod.rs` 172, 4053, 4117, 4138, 4201, 4290, 4337, 4511 and both `keystream.rs` sites 184, 314; `read_container_for` is itself `&self` ) -- and **3 need `&mut`**: `rebuild_allocator_at_open` ( 1248 ), `allocator().slab_count()` ( 2356 ) and checkpoint ( 2730 ), all open/stats/checkpoint paths, none on a query. So `.read()` at ten and `.write()` at three changes **nothing about when the lock is released** and needs no decision about holding it across the decode. Independently proposed by the consumer after the same elimination; unverified here.

  **But every measurement behind this entry is read-only with no writers**, including the consumer's, and that is the gap to close before converting anything. `RwLock` trades "readers serialize" for "**a writer may not acquire**", and the write side here is asymmetric in a way that matters:

  * `WriteBatch::commit` **never takes the store lock** -- it takes the memtable's `RwLock` and the WAL -- so the ordinary write path does not contend with reads at all.
  * **Checkpoint holds the store lock across its entire body** ( `mod.rs` 2730-2907: tree rebuild, superblock flip, deferred reclamation ). One long exclusive hold.

  `std::sync::RwLock` is not writer-preferring, and sustained full-occupancy reading is precisely the shape that starves a writer. **A starved checkpoint is not a latency problem**: checkpointing is what bounds WAL growth and what runs `reclaim_deferred`, so delaying it converts a throughput issue into a disk-space one -- a worse failure than the one being fixed. Measure a read/write mix, not only a read sweep, before believing any figure here.

  **A trap for anyone doing both halves: the inner `segs` / `pinned` / `verified` mutexes in `store/segment.rs` are redundant *only while the store lock is exclusive*.** `SegmentedMmap` is owned by value inside `ShardStore` and every production access is under `Mutex<ShardStore>`, so those three acquisitions per read currently guard against a race that cannot happen. Convert the store lock to `RwLock` and they immediately **become load-bearing**, because concurrent readers would then mutate `pinned` and `verified` at once. Removing them first and converting second gives data races on two `BTreeMap`s, and each step looks locally justified. See JOURNAL 2026-09-14.

  So the direction is reducing **acquisitions per read** or removing the shared mutex from the read path -- a read path that took `Mutex<ShardStore>` once per chunk plus three more inside `segment.rs` is acquiring four locks to move bytes that are already immutable under I2. Releasing the store lock before the decode remains sound ( the `Buffer` keeps its mapping alive independently, which is the whole purpose of `ExtentGuard` ) but is not by itself the fix.

  Do not start by converting locks in `store/segment.rs`. Three candidates were eliminated with one experiment each: the checksum cache's `verified` mutex ( 0.28x -> 0.30x ), `buffer_at`'s `pinned` mutex ( 0.28x -> 0.37x ), and making `segs` an `RwLock` because it is append-only, which had **no effect at all** ( 0.26x against 0.28x ). A lock that looks like contention is not, when something coarser upstream already serialized everything. -- *source: consumer measurement plus local reproduction, 2026-09-14*

### Replication

- [~] **ci-has-run-15-times-and-never-passed** **RENAMED AND CORRECTED 2026-09-13 — the premise was backwards.** `gh run list` reports **15 runs, every one a failure**: 12 of `ci` back to 2026-08-29 ( including scheduled ones ) and 3 of `release`. The entry said "still never executed remotely", which made the risk look like an unknown when it was a **known, reproducible, week-old red**. Nobody had run one command against the remote.

**Both causes found and fixed 2026-09-13.**

  * **clippy** — CI resolves `stable`, which is **1.98** here, while the repo's default toolchain is **1.97.1**. The failing lint `clippy::chunks_exact_to_as_chunks` **exists only in 1.98**, so `scripts/gate.sh` was structurally unable to see it and reported green through twelve red runs. A newer stable was already installed: `cargo +stable clippy --workspace --all-targets --all-features -- -D warnings` reproduces CI exactly, and is the command to use before trusting the lint gate. Most of the nine flagged sites had already been converted; one remained ( a constant-size block scan ) plus a genuinely duplicated import in a test module that 1.97 cannot see because the module's glob of its parent already supplies it. `as_chunks` is stable at MSRV **1.89**, verified by compiling a probe with that toolchain, so the rewrite costs no compatibility.
  * **AddressSanitizer** — CI ran `cargo test -p yesno-core` **without `--lib --tests`**, so doctests were included and could not *link*: the library is built with `-Zsanitizer=address` while rustdoc links examples against dependency rlibs built without the runtime, giving `undefined symbol: __asan_report_load8` before any code runs. **The local gate fixed this on 2026-08-30, with a comment explaining it and a `Do not "fix" this by deleting those doctests` — and the fix was never copied into the workflow.** That is this repository's recorded pattern ( a tool and its instructions are two implementations of one check ) recurring as a step and its CI twin.

**All seven CI jobs now have a local equivalent that passes**, checked job by job rather than assumed: `gate` ( routine gate green, **plus** `cargo +stable clippy` for CI's own lint version ), `msrv` ( `cargo +1.89 build -p yesno-core` ), `lean core`, `python client`, `java client`, `go client`, and `deep` — whose ThreadSanitizer and Valgrind steps were never the failing ones, and whose ASan step now carries the invocation the local deep gate has been passing with.

**The dependency guard passes with zero headroom**: `yesno-core` is at **5 direct** dependencies against a limit of 5 and **36** transitive lines against a limit of 36. So *any* new dependency in the core fails CI immediately, and the failure will read as a limit problem rather than as the design decision it enforces. Also verified clean: no `tokio`, `tonic`, `prost` or `arrow-flight` reaches `yesno-core`.

**A third defect, found only because a push produced no `ci` run at all.** Both workflows computed `concurrency.group` as `${{ github.workflow }}-${{ github.ref }}`, and in a **reusable** workflow `github.workflow` is the **caller's** name — so the called gate landed in the caller's own group and its `cancel-in-progress: true` cancelled the run waiting on it. `gate` failed in seconds, `plan` and `binaries` skipped on `needs.gate.result`, and no `ci` run existed to inspect.

**`ci.yml` has no `push` trigger**, so pushes are gated *only* through that call. From the first `release` run on 2026-09-06 to 2026-09-13, **every push to `main` ran no gate at all** — while the Sunday `schedule` trigger kept producing runs, because there `github.workflow` is `ci` and nothing collides. A weekly run and a badge are compatible with pushes being entirely ungated.

Fixed with a literal `ci-` group prefix plus `release.yml`'s tag guard. The next push confirmed the reusable call by showing nested `gate / <job>` entries inside the release run. **A reusable workflow must not derive its concurrency group from a context its caller controls.** GitHub exposed no diagnostic for the original failure — no annotations, no check-run output, and an empty log archive — so the diagnosis depended on the mechanism until the nested jobs appeared.

**CI PASSED at 2026-09-12T16:58Z — the first green run this repository has ever had**, after 15 consecutive failures. Every job of the `release` run succeeded: the whole gate through `workflow_call` ( `gate / gate`, `gate / msrv`, `gate / lean core`, `gate / python client`, `gate / java client`, `gate / go client` ), then `plan`, `compile (arm64)`, `compile (amd64)` and `assemble and publish`. `gate / gate` is the remote confirmation of the clippy fix, which no local run could give.

**One job remains unvalidated remotely: `deep`.** It is skipped on a push by design ( `if: github.event_name == 'schedule' || inputs.run_deep` ), so the AddressSanitizer scope fix has not executed on CI. It runs on the **Sunday 03:00 UTC** schedule, or immediately via a `workflow_dispatch` with `run_deep=true` — that job takes **hours**, so it is the operator's call. Until then treat the ASan fix as locally-evidenced only: the local deep gate passes with the identical invocation, which is why it was chosen, but that is the same class of argument this entry exists to distrust.

**Superseded note, kept for the record.** The reusable call itself is confirmed, and all three fixes are verified as far as the available evidence allows: the workspace is clean under CI's own clippy version, MSRV builds, the sanitizer invocation matches the locally passing deep gate, and nested remote jobs now execute. Do not close this until the complete job graph is green.

  * gate: the AWS runner scripts' POSIX-sh parse, Theorem 7's termination measure, and the derived-figures audit ( with `--self-test` first, exactly as the local gate does, because a constants audit that has silently stopped auditing looks identical to one that passes ).
  * deep: the aged-state space measurement and WAL bytes per ordinal. **That pair is the proof this mattered**: they had been failing in `--deep` for days because `yesno-e2e` gained binaries in `src/bin/`, and had CI run them it would have caught it. Their CI guard reproduces `report`'s semantics — non-zero exit *or* empty output fails the step — and was sabotage-checked in all three directions ( succeeds-with-output, non-zero exit, empty output ).

**Everything CI runs that can be run here was run here and passes**: all five `check-*.py` scripts, `cargo +1.89 build -p yesno-core` ( the MSRV promise — nothing else locally builds at 1.89, so today's changes could have broken it silently ), workspace clippy, `cargo fmt --all --check`, and the extracted WAL step executed verbatim out of the YAML. `release.yml`'s referenced files all exist and `build-release-image.sh` really does accept the `--binaries-only` and `--platform` flags the workflow passes.

**`lean-core` is at exactly its limits: direct 5/5, transitive 36/36.** Zero headroom — the next transitive dependency anywhere in that tree fails CI, and the failure will look unrelated to whatever PR adds it. Do not raise the numbers; that is what the guard is for.

**What still cannot be checked here**: completion of the GitHub job graph, caching, the remaining client-job toolchains, and the x86-64 arm of the deep job. The original entry follows for provenance ( *was `no-ci`; a workflow existed but was unproven* ): `.github/workflows/ci.yml` had four jobs — `gate` ( clippy, workspace tests, the layout check, R1, the docs checks, rustfmt scoped to the PR diff ), `msrv` ( `yesno-core` at 1.89 ), `lean-core` ( five direct dependencies, 36 transitive, and no `tokio` / `tonic` / `prost` / `arrow-flight` reaching core ), and `deep` ( ASan, TSan, Valgrind and — added 2026-08-30 — the two-node failover drill, on a weekly schedule and manual dispatch ). At that point **none of it had executed anywhere**. Every command had run locally and passed, which was exactly the evidence that was not enough: the failure modes of a workflow are the runner's, not the command's — a missing `openssl`, a target that will not install, a port the sandbox refuses, or a job that silently skips on the wrong event.

  The platform was not a guess after all: `Cargo.toml` declares `repository = "https://github.com/moriyoshi/yesnodb"`. The earlier note here — *"guessing a platform would be worse than naming the gap"* — was over-cautious, and cost the repo a gate for the whole of M0 through M7.

**Every command in it was verified locally on 2026-08-25; the workflow itself has never run anywhere.** YAML parses, the shell steps are syntax-checked, the dependency guards were executed verbatim, and the async guard was checked against `yesno-flight` to confirm it fires when a forbidden crate is present. What is unproven is everything only GitHub can exercise: action versions, cache keys, runner behaviour, and the `pull_request` fmt step ( untestable here — the repo has no commits, so there is no base to diff against ). Treat the first run as the test of this file.

  Two known differences from local verification: the runners are `x86_64-unknown-linux-gnu` where everything here was checked on `aarch64`, and `deep` installs `valgrind` via apt rather than using a preinstalled one.

Do not add `cargo fmt --check` over the whole tree — 89 pre-existing hunks would make it red on arrival, which trains people to ignore the gate and reproduces the exact failure it exists to fix. See `fmt-baseline`. -- *source: 2026-08-25*
- [x] **paged-leaf-stream** ( **DONE**; landed with `Snapshot::key_stream` 2026-09-13, verified closed 2026-09-14 ): `KeyStream` decodes containers from the mmap one at a time, taking and releasing the store lock per chunk, and `ChunkStream::stats` reports `Backing::Paged` while any remaining chunk still needs a read ( `keystream.rs` ) -- the first leaf in the crate ever to report it. It reaches the planner: `snap.key_expr( k ).open().stats().backing` is `Paged`, asserted directly, and `yesno-flight/src/expr.rs` records it as "what finally puts `Backing::Paged` in front of the planner".

  The entry's premise -- "`Backing::Paged` is reported by no production code", so the `stats` channel is "correct but under-exercised outside tests" -- was true when written on 2026-08-26 and stopped being true on 2026-09-13. It was still open on 2026-09-14 because nothing rechecks a negative existence claim once the thing starts existing. -- *source: 2026-08-26; closed 2026-09-13*
- [ ] **segmented-and-with-shared-cursors** **RE-CHECKED 2026-08-27 — the prize it names is ~13x smaller than recorded, and it conflates two different savings.** `and_shape.py`'s `identical` regime exists for this question ( same set for every operand, so nothing shrinks and any cost is overhead plus a trivial kernel ): **18.0 ns** per `And` per chunk at k=4, **17.4 ns** at k=8, flat in k — against the recorded "~240 ns/chunk of peek/seek/dispatch". The same run shows where 240 could have come from: the `dense` regime ( 50 000 ordinals/chunk ) costs **180 ns per operator per chunk** — but that is **kernel time, not dispatch**, and this item attributes its figure to dispatch by name.

**Two prizes, different sizes, and the item merges them.** ( a ) Not running the *kernel* — `x ∩ full = x` — saves the whole per-chunk cost wherever containers are large ( ~180 ns/chunk on `dense` ). Real and large, and it needs only a `Full` test, which `ChunkRef::is_full` already answers `O(1)` from the index. ( b ) Not running the *plumbing* — peek/seek/dispatch — saves ~18 ns/chunk/operator, and that is the shared-cursor operator the title asks for. **The money is in ( a ), which is much cheaper to build.** Interacts with `aligned-chunk-views`, which claims to subsume this item and whose own margin has likewise been taken by the engine getting faster; both budget against per-chunk costs measured before the planner walk went linear. -- *2026-08-27*

  **segmented-and-with-shared-cursors** ( original entry ): `And` has no segmentation, so a query like `( B ∪ C ) ∩ A` cannot exploit `A`'s run structure. With `A` 0-filled over `[0,99]` and 1-filled over `[100,199]`, the ideal plan never evaluates `B ∪ C` outside `[100,199]` and does no intersection kernel work inside it ( `x ∩ full = x` ) — the whole query becomes a restricted union. Leapfrogging already skips the 0-filled part, so the missing win is the 1-filled part and the per-chunk operator overhead ( measured ~240 ns/chunk of peek/seek/dispatch that a run-level pass-through would avoid ). Do **not** build it on the `segmented_or` pattern of re-opening operands per segment: `And`'s operands are typically composite, so re-opening duplicates whole evaluations. It needs an operator that owns both streams and switches strategy per segment against a single cursor each — which is also how `segmented_or` should eventually work. See `docs/formal-model.md` §14 for the restriction algebra this would implement — Prop 18 says the restriction pushes into both operands with no side condition, Prop 21 says which leaf collapses pay. The cut points worth trying first are `Full`/non-`Full` boundaries of an `And` operand, because such a cut deletes an entire operand ( Prop 21 case 3 ). And note the measured result that an n-ary `IntersectAll` beats both tree shapes by 1.3-1.7x on small containers — if `And`'s per-operator overhead is the dominant term, a shared-cursor n-ary operator may deliver most of this item's value without any segmentation at all. That win is unconditional **only with an eliminating early-out** — a converge-then-fold n-ary is 11x slower where intersections are empty ( see `aligned-chunk-views` ). A shared-cursor operator for this item must fold eagerly and stop on empty, which is the specification of the thing that makes it win rather than a caveat about when it does. A segmented `And` that advanced every operand across every segment would reproduce the 11x *inside* the segmentation machinery. -- *source: 2026-08-26* This is also the only thing that would make `ChunkProfile` pay: measured on ragged mixed-kind operands, whole-expression `covers()` fires **zero** times, because it needs `Full` across the other operand's entire span, and raggedness destroys whole-span coverage. Per-segment, `A` *is* full over `[100,199]` and the intersection there becomes a pass-through.
- [ ] **split-as-a-rewrite-not-a-lowering**: segmentation happens in `open_planned` and produces *streams* ( `Restrict` / `Concat` ), so the rewrite rules — which run in `plan()` beforehand — never see a segmented sub-expression. That is why `covers()` / `ChunkProfile` fires zero times on ragged input: it only ever sees whole-expression spans. **Demonstrated**: hand-writing the split with the restriction pushed down into both operands makes the absorption fire and delete the intersection outright ( `And(And(Or(B,C), Range(100..200)), A)` -> `And(Or(B,C), Range(100..200))` for `A` 1-filled over `[100,199]` ). Making splitting an `Expr` -> `Expr` rewrite would let `plan()`'s existing 8-pass fixpoint alternate split and rewrite. `And( x, R )` already *is* an Expr-level restriction, so no new variant is needed — but the existing absorption removes `R` when `x ⊆ R` while a pushdown inserts it, so the two oscillate unless the pushdown is gated to fire only when it strictly narrows a span some rule can exploit; and a pushdown grows the tree, so it needs its own decreasing measure for termination. -- *source: 2026-08-26* **Design explored 2026-08-26** in `docs/split-merge-algebra.md` ( 1 045 lines, by the agent in the neighbouring session ). **That document was deleted on 2026-08-27**, after the `aligned-chunk-views` direction superseded its §§3-10 and its §2 was migrated into `docs/formal-model.md` §14. The repo had no commits at the time, so it is not recoverable from git — **this entry is the record of §§3-10**, together with the cut ranking on `segmented-and-with-shared-cursors`, the acceptance-gate consequence on `disjoint-or-is-overcharged`, and the allowance conjecture below. Its conclusions: put split **above** the rules as strategy-local state, not in `pass()` and not at lowering; make pushdown a total structural recursion rather than a rewrite rule, which keeps it out of the fixpoint and bounds growth at `|e| + 2L`; `PlanStrategy`'s existing signature is then sufficient. **Its first termination proof was wrong and the correction is the load-bearing part.** The measure must be read off the **committed** cut set `Pi` ( strategy state ), never off the **emitted** segmentation `Sigma` ( what fusion produces for the output ). Fusion removes segments, so a measure on `Sigma` is not monotone and admits `plan -> split -> plan -> merge -> split`. Concrete livelock: `And(A, B)` with `A` `Present` over `[0,c)` / `Full` over `[c,d)` and `B` `Empty` throughout — the top-ranked cut `c` is split, both segments annihilate through `B`, the bodies become congruent, fusion merges them, and if fusion defines the next partition the same cut is selected forever with the expression stable, so `same_shape` cannot see it. **Rule: fuse into the returned `Expr`; leave the `Vec<Window>` that drives the loop alone.** The bug is invisible whenever no fusion fires ( `Sigma == Pi` ), so it diverges only on the ragged multi-operand inputs the design exists for — pin it with a one-line invariant that the committed partition is non-decreasing across rounds. Also do not take the doc's single `for` pass over ranked cuts as the fix: it terminates trivially and would hide a write-back bug while producing worse plans; a genuine fixpoint must re-offer rejected cuts and terminates only under the monotonicity hypothesis. -- *2026-08-26*
- [ ] **aligned-chunk-views** **RE-CHECKED 2026-08-27 — its measured case was computed against an engine that is now 7-26x faster, and three of five rows invert.** The prototype survives and still runs; the A/B/C corpus is unchanged ( per-query segment counts reproduce exactly: 6, 5, 3, 6, 4 ), so the `engine` column is comparable. The sieve rows are **not** — the prototype now runs 50 chunks against the recorded 200. Engine now: `( B u C ) n A` **24.80 -> 3.57 us**, `B n A` 2.08 -> 0.27, `C \\ A` **20.65 -> 0.79 ( 26x )**, `( B ^ C ) n A` 20.77 -> 2.38, `( B u C )` 15.73 -> 2.00. Since `total x = engine / ( prep + walk )` ( verified — it reproduces all five recorded ratios ), holding the denominator gives **11.07x -> 1.59x, 4.95x -> 0.64x, 1.84x -> 0.07x, 1.20x -> 0.14x, 0.68x -> 0.09x**: the headline drops to ~1.6x and the single acknowledged loss becomes four.

**A projection, not a measurement.** The prototype now reports the walks as **work counters rather than a clock** — deliberately and correctly, since `CLAUDE.md` forbids a wall-clock column for a Python walk — so `prep`/`walk` cannot be re-read in microseconds and are assumed unchanged. And the recorded ratios were always **a Python walk timed against a Rust engine**, i.e. cross-language, which is presumably why the fixture was revised. **What survives is what was stated as counts**: `B n A` still touches **zero** positions, and the exactness of the three-valued classification is an argument rather than a timing. Not "the design is dead" — the margin it was budgeted against has largely been taken by making the existing engine faster, so it must be **re-prototyped against the current engine before being built**, which is what "Prototype before committing" already said, now with a reason. -- *2026-08-27*

  **aligned-chunk-views** ( original entry ) ( supersedes the support-set framing of the same morning, and the `intersectall-with-early-out` framing before it ): present every leaf as a view that classifies its domain `Empty` / `Full` / `Present(container)`, co-iterate the operands' boundaries, and evaluate the expression once per segment of constant classification.

**"Grid" is the wrong word and the maintainer withdrew it; the idea survives the correction.** There is no grid to align to — the domain is `2^48` positions and nothing enumerates it. What happens is a **lazy co-iteration over each operand's own boundary sequence**, taking the minimum; positions are discovered, not indexed. Nor are boundaries chunk boundaries: a `Range` has them at arbitrary *ordinals*, and the prototype quantized those onto chunk edges by calling partial end chunks `Present`, which discards "full from ordinal x onward within this chunk". **The chunk is the atom of payload work, not of reasoning** — a container cannot be cut at an arbitrary point, so sub-chunk boundaries fall back to a kernel, and `range_summary` / `len_in_range` are the kernels that already answer sub-chunk questions without materializing. What survives the correction is the part that does the work: a three-valued classification exact under the operators, plus run structure so uniform stretches collapse. Neither needs a grid. A `Range` synthesizes partial first/last chunks and `Full` interiors arithmetically, so nothing is materialized and the `Range(0, 2^64)` problem that sank the support-set representation does not arise.

  **The early-out becomes a consequence rather than a feature.** At each position the tree is evaluated on the three-valued markers *before* any payload is touched — `Empty ∩ x = Empty`, `Full ∩ x = x`, `Full ∪ x = Full` — which is exact, not conservative, and free: `Full` is `card_m1 == 0xFFFF`, an `O(1)` index read the format already guarantees, and `Empty` is absence.

  **On the motivating query it produces the ideal plan with no planning at all.** `( B ∪ C ) ∩ A` with `A` 0-filled over `[0,99]` and 1-filled over `[100,199]`: positions 0-99 evaluate to `Empty` and are skipped without a container touch; positions 100-199 reduce to `B ∪ C` with no intersection kernel. That is exactly what `segmented-and-with-shared-cursors` asks for, reached without a rewrite, a statistic consulted for gating, or a segmentation decision.

**The grid must be coarsenable, and that is where the performance is.** If positions are always single prefixes, ragged input costs a tree walk per chunk. If boundaries are instead the coarsest positions where *every* operand's classification is constant — the merge of all operands' run boundaries — uniform stretches advance in `O(1)` and cost is paid only at real transitions. Note what those boundaries are: they are the split/merge design's frozen cut set `X` ( the cut set `X` of `docs/formal-model.md` §6.3, generalized from span endpoints to run boundaries ), **discovered lazily by walking rather than computed and committed**. That removes that design's central problem outright — no committed partition, so no fusion write-back, no monotonicity hypothesis, no livelock, and forward motion is its own termination argument. The 1-filled absorption fires per position as `x ∩ Full = x` without being a rewrite rule.

**Interpretive overhead does not amortize here the way it does in a columnar engine.** There, per-tuple interpretation is spread over millions of rows; here a position is a chunk and there may be only hundreds, so walking the tree per position is a real cost. Flatten the tree once into a small stack machine and run that per position — in the design from the start, not retrofitted.

This is a second execution model, not a local change: `ChunkStream` is pull-based per-operator and an aligned evaluator is a different shape. They can coexist ( the evaluator is one `ChunkStream` at the root ), but it would subsume `segmented_or`, most of `concat_disjoint_or`, `segmented-and-with-shared-cursors`, the n-ary-operator direction, and most of the retired split/merge design. Prototype before committing. Its correctness argument is `docs/formal-model.md` §14, and **Prop 20 clause 2 is the trap**: this evaluator's boundaries are run boundaries, not prefix boundaries, so `Concat` is *not* a valid reassembly — two adjacent segments can contribute to the same chunk. Use the single-scratch-container append §14.3 describes.

  **This decides `statistics-earn-nothing`.** `ChunkProfile` already *is* per-operand `Empty` / `Full` / `Present` runs, so merging `k` of them yields the aligned segmentation directly. The statistics are not for gating rewrites — where they have measured as worthless three times — they are for driving the walk. Do not delete `ChunkProfile` or `PrefixOccupancy` until this is settled.

  **Prototyped and measured 2026-08-26** as `.agents-workspace/tmp/prototypes/aligned_eval.py` — **scratch, gitignored, and may be deleted at any time; this entry is the durable record and is written to be sufficient without the file** ( it was `examples/aligned_eval.rs`, then `e2e/scenarios/`, before the maintainer classified it as a prototype rather than a test on 2026-08-26 ), outside `src/` on purpose. Answers asserted against the engine at every row.

```text
  query                    engine     prep     walk    walk x   total x   seg  touched
  ( B ∪ C ) ∩ A             24.80     1.46     0.78     31.6x    11.05x     6       40
  B ∩ A                      2.08     0.37     0.05     44.4x     5.01x     5        0
  C \\ A                     20.65     1.24    10.01      2.1x     1.84x     3      900
  ( B ⊕ C ) ∩ A             20.77     1.46    15.78      1.3x     1.20x     6      100
  ( B ∪ C )                 15.73     1.31    21.80      0.7x     0.68x     4      890
  sieve k=8                 25.95     2.59    17.52      1.5x     1.29x   200       47
  sieve k=32                93.93    12.38    70.70      1.3x     1.13x   200       36
```

  **The motivating query is 11x end-to-end and 32x on the walk**, and `B ∩ A` touches **zero** positions — the model does exactly what it promised. The single loss is `( B ∪ C )` alone at 0.68x, and it is diagnostic rather than fatal: a pure union where every position is `Present` has nothing for abstract evaluation to eliminate, so the interpretive walk pays for machinery it cannot use. That condition is detectable up front ( no operand has `Empty` / `Full` runs ), which is the obvious fallback to the existing merge.

**Two prototype artifacts cost 3-20x before they were found, and both are the same mistake this codebase keeps making.** Binary-searching the run array per position, where the walk is strictly forward and wants a monotone cursor — the very thing `SetStream::seek` gallops for — cost 780 ns per segment on segments that do nothing. And returning owned containers, where the engine's kernels take references, cost a payload clone per leaf per position. Fixing both took sieve k=32 from 0.6x to 1.3x and `( B ∪ C ) ∩ A` from 13x to 32x. Do not read the first numbers from a prototype of this as the model's numbers.

  **`prep` is the open question, and it is the `O(chunks)` term that has twice measured as not paying.** It is 5-15% of engine time on the winning rows but 13% of a 94 us query at sieve k=32, and it scales with total chunk count. It is also the one cost that is *amortizable*: the classification is per-`OrdSet`, not per-query, and is either `ChunkProfile` memoized or derivable from the index via `card_m1` with no payload touched. With prep free, sieve k=32 is 1.33x and the motivating query 32x.

  **Two consequences of dropping the grid, both improvements, both unmeasured.** ( 1 ) Boundaries become **demand-driven**: with no grid there is no classification array to build, each operand answers "next boundary at or after `p`" from a cursor, and cost is paid only for boundaries actually reached. That is where `prep` goes, and it dissolves the representation question below rather than answering it — there is nothing to precompute. ( 2 ) **The prototype cuts each segment at the minimum boundary over *all* operands, including ones whose classification did not determine the answer.** On `( B ∪ C ) ∩ A`, `C` has 1000 chunks contributing boundaries throughout `[0,99]` where `A` is `Empty` and the result is `Empty` regardless. The segment should extend to the next boundary among the operands that **decided** the answer — `A` alone, there. That is the maintainer's earlier "ruling sets" idea recovered per segment and dynamically rather than globally and statically, and it is the eliminator idea from the k-way literature one level up. It is also what makes demand-driven boundaries pay: `C` is never asked for a boundary in a region `A` has already settled.

  Predictions worth testing together, since ( 2 ) is what makes ( 1 ) worth having: `prep` collapses toward zero on operands with few boundaries ( `A` ) and does **not** collapse on operands whose classification alternates every few prefixes ( `sieve`, ~200 boundaries per operand ) — there the work moves from `prep` into the walk and the total is roughly unchanged. So the predicted gain is concentrated exactly where the model already wins, which is a reason to be suspicious of it and to measure rather than assume.

  Superseded, and recorded only so it is not re-asked: whether the classification is built from `ChunkProfile` runs ( cheap, exact for run-structured operands, degrades to a single `Present` run above `MAX_PROFILE_RUNS` so chunk-level raggedness gets nothing ) or from the prefix arrays directly ( exact everywhere, `O(chunks)` ). The prototype builds it directly and pays for it in `prep`. -- *source: maintainer, 2026-08-26; prototyped 2026-08-26*
- [ ] **allowance-starves-the-rewrites-worth-most**: the statistic allowance is `min( STATS_BUDGET, cheap_yield(e) * STATS_HEADROOM )`, and `cheap_yield( And ) = min(...)` — which estimates *execution* cost correctly but not *statistics* cost, which scales with operand size. So the budget is smallest exactly for the rewrites that eliminate a large operand, the ones with most to gain: the 1-filled absorption above fires only with an unbounded allowance. An `O(1)` necessary condition ( `could_be_full_over`: being full over `n` chunks needs `n * CHUNK_CARD` ordinals, and cardinality is cached ) recovered the isolated case, but the full pushed-down expression still exhausts the budget. Neither `min` nor `max` resolves it — `max` restores the measured 34x pessimization on `4000c AND 2c`. Needs a benefit estimate that accounts for what a rewrite would *remove*, not just what the query currently costs. -- *source: 2026-08-26* A conjectured reshaping, from the retired split/merge design and recorded here because that document is gone: under a split-based strategy the profile is built once per leaf and its transitions *generate* the candidate cuts, so every profile built is used, and the leaf collapse ( now `docs/formal-model.md` §14.4, Prop 21 ) supplies a removal-denominated benefit estimate ( a `Full` run of width `w` in an `And` operand saves ~`w * MERGE_STEP` ). Explicitly flagged there as economics-by-structure, unmeasured, and the same class of claim this project keeps measuring as wrong. -- *2026-08-26*
- [ ] **memoize-loads-not-statistics**: memoization belongs **below** the planner, not in it. Measured cost of `Snapshot::load( key )` against deriving *all three* statistics from the resulting set: **4.5x at 100 chunks, 6.2x at 1 000, 16.1x at 10 000** — materializing the set dominates, and the gap widens with size. Every memo tried in the planner cached the cheap thing while its input was re-derived from scratch on the next call. `Snapshot::load` is `OrdSet::from_chunks( self.merged_chunks( key ) )` with **no cache anywhere in `Db`**, so two loads of the same key at the same version produce distinct allocations — which is also why a strategy-level memo has nothing to hit. The natural key is `( key, version )`: under MVCC that pair denotes an immutable value forever, versions are monotonic and never reused ( so no ABA ), and invalidation is the lifecycle the crate already runs — entries below the version watermark become unreachable exactly as extents do. Once `load` returns a shared `Arc<OrdSet>`, the planner's memo can be the simplest possible thing ( per-call, pointer-keyed, zero invalidation ) and will finally hit, because the pointers recur. Not free: a set cache needs a memory bound, an eviction policy, and to interact correctly with `evict_floor` / snapshot retention. -- *source: 2026-08-26*

**RE-MEASURED 2026-08-27 — the ratio does not widen, it inverts, and the quantitative case is gone.** `load_vs_statistics` in `benches/setops.rs`: `load` against deriving `PrefixSketch::build` + `ChunkProfile::of_set` + `exact_shared_prefixes` from the result gives **1.16x / 1.01x / 0.79x** at 100 / 1 000 / 10 000 chunks, against the recorded 4.5x / 6.2x / 16.1x. At 10 000 chunks the statistics now cost **more** than materializing the set. I cannot reproduce the original denominator — "all three statistics" is unspecified — so those two columns may not be the same measurement and I am not claiming a like-for-like 16.1x -> 0.79x. **What survives regardless is the shape**: `load` is now linear and every statistic is linear, and *two linear quantities cannot have a ratio that widens with size*. The widening was the `BTreeMap` in `merged_chunks`, and it is gone. The entry's **structural** argument is untouched: `load` has no cache anywhere in `Db`, so a planner-level memo has nothing to hit until it returns a shared `Arc<OrdSet>`. That is still the reason to do this; "materializing the set dominates" no longer is. Lesson: **a ratio recorded without its denominator's definition cannot be re-derived, only re-measured.** -- *2026-08-27*

*Superseded note, kept for the derivation.* **Re-measure before building this: its motivating ratio has moved.** The 4.5x / 6.2x / 16.1x above is `load` against statistics derivation, and `load` got **1.30x / 1.73x / 2.29x** faster at 100 / 1 000 / 10 000 chunks when `load-is-superlinear` closed on 2026-08-26 — the widening with size, which is the part of the argument that says "memoize the load, not the statistics", was the `BTreeMap` and is gone. The conclusion probably survives ( materializing a set still dominates deriving three statistics from it ) but the *number* that justified it does not, and this item's own point is that a memo built on a mismeasured input caches the wrong thing. -- *2026-08-26*
- [ ] **lazy-view-expression-nodes** ( *2026-08-30* ): the first Flight integration deliberately treats `ViewSelect`, `ViewFold`, and `ViewExpand` as eager transform boundaries, then re-enters the Boolean expression as a materialized set leaf. A top-level select still has a dedicated exact-cardinality path. This preserves the audited `Expr` planner and termination proof while exposing the complete view semantics, but a view transform nested under Boolean operators can allocate its full logical result. Add core `Expr` variants only after a workload or allocation fixture demonstrates the cost and the planner's bounds, statistics, streaming cardinality, and termination measure have all been extended together. A server-side view descriptor catalog is a separate feature and must not be smuggled into this item: descriptors are caller-owned request metadata today.
- [~] **agent-mount-propagation-under-a-real-container-runtime** **The mechanism half is closed 2026-09-08 by `scripts/gate-mount-propagation.sh`.** The entry's load-bearing uncertainty was *"nothing tests that Docker's or Kubernetes' flags actually produce it"*. They do, and it is now gated:

  ```text
  agent   container, CAP_SYS_ADMIN, snapshot root bound rshared
  host    observes a mount the agent makes, and reads through it
  daemon  container, uid 10001, snapshot root bound rslave, started FIRST
  ```

  Eight checks, including that an unmount propagates back in both directions. The daemon container is created **before** any mount exists, because a namespace only receives mounts made after it was created — a daemon started afterwards inherits a copy of the table and passes whatever its propagation says. Sabotage-checked: `rshared` -> `rprivate` breaks host visibility, `rslave` -> `rprivate` breaks only the daemon, removing the mount breaks both.

**Two deployment requirements it surfaced, neither of them optional:**

  * **`CAP_SYS_ADMIN` alone is not enough on an AppArmor host.** Docker's default profile denies `mount( 2 )` even with the capability — the agent fails `Permission denied`. `--security-opt apparmor=unconfined` or an equivalent profile exception is required, and a Kubernetes deployment needs the same. The gate reports this case explicitly so the requirement cannot quietly stop being true.
  * **The snapshot root must be traversable by the daemon's uid.** `mktemp -d` gives 0700 and the daemon then cannot enter the mount *after receiving it* — the same failure `lvm_propagation.py` records for the agent's pre-mount directory. Do not resolve a traversal failure by running the daemon as root; that is the thing under test.

**Still open: the same topology carrying the real processes.** This gate proves the *runtime* produces the propagation; it does not run `yesnod` and the snapshot agent in those containers, so the lease path across a container boundary is still only covered by the systemd equivalent in `lvm_propagation.py`. Local EBS materialization needs that same gate — its AWS runner uses one privileged container for both processes, so it proves the process split only. Original entry: **NOT blocked on hardware — verified buildable here 2026-09-08.** A sweep classified this as needing a container runtime this machine does not have. It has one: `docker info` succeeds, `/dev/kvm` is present, `scripts/gate-filesystems.sh` runs green ( `zfs.py`, `btrfs.py`, `lvm.py`, `lvm_propagation.py`, 4 passed ), and the exact mechanism this item needs — a privileged container with an `rshared` bind — was confirmed working. Do not re-file this as environment-blocked. It is **unimplemented, and sizeable**: a second gate that starts two containers ( agent with `SYS_ADMIN` and an `rshared` snapshot root, daemon unprivileged and separate ), comparable in scope to `gate-operator.sh`, not a small fix. Original entry: ( *2026-09-01*,
  narrowed from `lvm-agent-live-container-mount-propagation-gate* ):
  `e2e/filesystems/lvm_propagation.py` now covers the receiving half — the
  daemon runs under `PrivateMounts=yes` ( a slave mount namespace, which is
  what `bind-propagation=rslave` and `mountPropagation: HostToContainer`
  produce ), the privileged agent mounts the clone in the host namespace, and
  the lease is only serviceable if the mount propagated. Sabotage-checked with
  `MountFlags=private`.

**What is still uncovered is the sending half under a real runtime.** A
  measurement while building that gate: systemd's `PrivateMounts=yes` plus
  `MountFlags=shared` does **not** propagate the agent's mounts to the host —
  a shared namespace gets a fresh peer group, so the mount stays inside it.
  Only an `rshared` bind of the snapshot root into the agent puts its copy in
  the host's peer group, and nothing tests that Docker's or Kubernetes' flags
  actually produce it. Add an opt-in gate that runs the agent in a container
  with `SYS_ADMIN` and an `rshared` snapshot root, and the daemon in a separate
  unprivileged container, under the documented security profile. Local EBS
  materialization needs the same gate: its AWS runner uses one privileged
  container for both processes, so that gate proves the process-level split
  only.

- [ ] **btrfs-provider-seam** ( *2026-09-01*, supersedes
  `btrfs-uapi-snapshot-provider` from *2026-08-31* ): **The `btrfs-uapi`
  adoption decision is deferred, not pending.** Do not re-evaluate the crate as
  the next step; the likely direction is our own seam, with any typed-ioctl
  implementation — third-party or in-house — chosen behind it afterwards.

  The evaluation still stands on its facts: `btrfs-uapi` 0.13 exposes safe typed
  snapshot, read-only-flag, delete and sync ioctls and meets the workspace MSRV.
  What made it not worth deciding now is the cost of the *dependency*, not of
  the ioctls: Linux-only, first published in 2026, bindgen and therefore
  libclang added to the build contract, and all of it landing on the privileged
  path. An owned seam separates the two questions — "what is the provider's
  interface to Btrfs" from "what implements it" — and lets the second be
  answered later, or differently per platform, without re-opening the first.

  What the seam has to cover, which is more than when this item was written:

  - create a read-only snapshot, and delete a subvolume;
  - **clear the read-only property and retry the delete when the kernel
    refuses it** — an unprivileged owner cannot delete a read-only subvolume
    even with `user_subvol_rm_allowed`, and this is the case
    `gate-filesystems.sh` now covers. Any implementation must be exercised
    under the delegated account, not only as root, or it regresses it silently;
  - the reconciliation walk over the snapshot directory, which is plain
    `readdir` and needs nothing from Btrfs at all.

Two properties of today's shell implementation are worth preserving
  deliberately, because both are easy to lose in a rewrite. The retry fires on
  **any** delete failure rather than on a matched error string, so it does not
  depend on `btrfs-progs` message wording. And no `unsafe` is involved: an
  in-house ioctl seam would introduce some to the server, which brings the
  unsafe rules with it — a `// SAFETY:` comment stating the invariant, a
  JOURNAL note, and a test that would fail if the invariant were violated.
  Against that, shelling out makes `btrfs-progs` a deployment dependency of the
  image, which the seam could eventually remove.

### Follow-ups consolidated on 2026-09-06

- [ ] **ebs-backend-live-xfs-gate**: extend the live local-EBS fixture from ext4 to XFS and exercise snapshot, attach, mount, restore, release, and reconciliation against the second supported filesystem. -- *source: 2026-09-02 AWS live-run sequence*

- [ ] **operator-ebs-daemon-snapshot-lease-live-coverage**: add the narrow control-plane verb that begins and releases a snapshot lease through `kubectl port-forward`. The green operator arm proves volume discovery and generated configuration, but `reconcile_errors == 0` is one-sided and does not prove the daemon reached EC2 through its IRSA identity. -- *source: 2026-09-05 operator EBS work summary*

- [ ] **aws-gate-full-four-arm-run** ( *no longer blocked by orphaned volumes -- the account was verified empty of them on 2026-09-12, so `aws_materializer_volumes( 0, 0, 0 )` is satisfiable again* ): run local EBS, deferred ECS, deferred EKS, and the operator arm in one invocation. Each arm has passed, but only in partial combinations, so no run yet proves their shared stack and cleanup compose. -- *source: 2026-09-05 AWS gate work summaries*

- [ ] **unified-image-replaces-per-role-builds**: update the operator harness and deployment inputs to consume the unified product image, then retire the per-role Dockerfiles only after the existing kind assertion is preserved. -- *source: 2026-09-06 published-image work summary*

- [ ] **release-image-filesystem-userland** **premise verified 2026-09-08**: `dist/Dockerfile` really does carry a dedicated `lvmtools` stage fetching `lvm2 dmsetup` for the target architecture, and neither `btrfs` nor ZFS userspace appears anywhere in it. Note the distinction that makes this survive a green filesystem gate: `gate-filesystems.sh` exercises btrfs and zfs **inside a KVM guest in the all-in-one E2E image**, which is a different image from the shipped one — so those scenarios passing says nothing about what an operator's deployment has. What remains is a product decision ( bundle the tooling into every image, ship a variant, or document it as the operator's responsibility ), not a test. Original entry: decide and test how deployments using the Btrfs and ZFS snapshot providers obtain their required userspace. The unified image includes LVM tooling but neither `btrfs` nor ZFS utilities. -- *source: 2026-09-06 snapshot-agent image entry*

- [ ] **archive-reclamation-conditional-store-coverage**: extend the deployed Winterbaume S3 fixture to exercise retention reclamation under conditional object-store semantics. Existing reclamation E2E uses `file://`, whose kernel lock bypasses the compare-and-swap path. -- *source: 2026-09-06 PITR work summary*

### Product direction: context eligibility and governed sharing

- [ ] **context-eligibility-service-contract**: write the narrow service contract for yesno as an operational context-eligibility plane rather than a bitmap DBaaS, OLAP engine, vector database or general authorization graph. Specify `filter_candidates( candidates, policies, minimum_versions ) -> mask + receipt` as the first product primitive, the ownership of ranking and content retrieval outside yesno, and the admissible failure semantics. Cross-reference rather than duplicate `no-session-guarantees-on-the-flight-surface`, `no-idempotency-key-on-the-write-path` and `no-conditional-write`; those three are prerequisites. **Partly satisfied 2026-09-12**: a receipt can now name a commit identity, because `do_put` reports its version and a read can be bound to it. Still outstanding for a receipt are the *minimum*-version request form, the idempotency key and the conditional write. Do not add core API until the contract identifies a workload and the layer that can test it. -- *source: product architecture review, 2026-09-12*

- [ ] **tenant-and-identity-namespace-model**: define how tenant identity participates in storage keys, authorization, quotas and audit, and how two parties prove that an ordinal names the same resource before cross-tenant algebra is permitted. The first version should admit only natural shared identifiers or platform-issued identifiers; raw hashes of enumerable personal identifiers are explicitly insufficient. Compare physical database-per-tenant plus a broker with logical multitenancy, and name which boundary enforces isolation below every parser and protocol. -- *source: context-eligibility product direction, 2026-09-12*

- [ ] **governed-share-capability-model**: design an explicit publisher-owned share binding owner, grantee, source expression, identity namespace, permitted operation, output policy, expiry, revocation state and version policy. Authorization must be rechecked at result delivery so a revoked share cannot be exercised through an old Flight ticket, snapshot lease or cached plan. Treat online filtering, enumeration and portable bitmap export as three different capabilities; an exported copy cannot be revoked. Start with same-organization shares, then publisher-to-consumer shares, before general cross-organization composition. -- *source: context-eligibility product direction, 2026-09-12*

- [ ] **cross-tenant-filter-disclosure-model**: threat-model what a consumer can infer by repeated candidate filters, singleton probes, complements and differenced counts. Define the first release's minimum candidate and result thresholds, query and rate budgets, permitted expression shapes, audit requirements and purpose binding. The initial surface should exclude unrestricted enumeration, export, complement and count; candidate-only filtering reduces disclosure but is not itself a privacy guarantee. Do not introduce differential privacy or cryptographic private-set intersection until a concrete customer requirement establishes the disclosure budget and trust model. -- *source: context-eligibility product direction, 2026-09-12*

- [ ] **versioned-decision-receipts-and-source-watermarks**: specify a receipt that records the consumer snapshot, every share and share version, the authorization grant, evaluation time and each source-ingestion watermark. Independently updated tenants do not have one naturally atomic global version, so the receipt must carry a version vector rather than inventing one. Choose one initial source path ( PostgreSQL outbox/CDC or Kafka ), make its offset-to-yesno-version mapping visible, and include a complete reconciliation and rebuild workflow. -- *source: context-eligibility product direction, 2026-09-12*

- [ ] **byoc-first-product-envelope**: define the initial supported commercial deployment as vendor-supported BYOC over rebuildable derived membership data. State the supported cloud, filesystem, upgrade, backup, fencing and recovery envelope and the division of operator responsibility. Defer multitenant DBaaS until dedicated deployments have produced measured workload limits, p99 latency, RPO/RTO, tenant-isolation evidence and a working release pipeline. This is a product decision and documentation task before it is deployment machinery. -- *source: product architecture review, 2026-09-12*
